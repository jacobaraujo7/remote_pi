{
  "Metadata": {
    "titleDefault": "Remote Pi — Your coding agents, in your pocket",
    "titleTemplate": "%s · Remote Pi",
    "description": "Pair your phone once, then drive any Pi coding agent from it — keep a fleet running 24/7 and link every machine into one mesh. Open source, self-hostable."
  },
  "SiteHeader": {
    "homeAriaLabel": "Remote Pi — home",
    "navCockpit": "Cockpit",
    "navTutorials": "Tutorials",
    "navDocs": "Docs",
    "navDownload": "Download",
    "navGithub": "GitHub",
    "install": "Install",
    "openMenu": "Open menu",
    "closeMenu": "Close menu"
  },
  "SiteFooter": {
    "copyright": "© {year} <b>Flutterando</b>. Remote Pi is open source under the MIT license.",
    "cockpit": "Cockpit",
    "download": "Download",
    "terms": "Terms of Service",
    "privacy": "Privacy Policy",
    "protocol": "Protocol",
    "github": "GitHub"
  },
  "LanguageSwitcher": {
    "label": "Language"
  },
  "Hero": {
    "eyebrow": "Open source · self-hostable",
    "titleLine1": "Your agents,",
    "titleLine2": "in your",
    "titlePocket": "pocket.",
    "sub": "Pair your phone once — then drive any agent, keep a fleet running 24/7, and link every machine into one mesh.",
    "install": "Install",
    "github": "GitHub",
    "metaInstall": "one command to install",
    "metaAccounts": "no accounts",
    "metaLicense": "MIT licensed",
    "demoAgentsMesh": "4 agents · 1 mesh",
    "demoDaemon": "daemon · answers at 3am",
    "demoChatTitle": "Frontend",
    "demoChatHost": "MacBook",
    "demoChatState": "working…",
    "demoUserMessage": "hello",
    "demoMdMessage": "This is a Pi Agent Network pairing code. `Frontend` is asking to connect to this machine — processing the link now.",
    "demoWaiting": "Waiting for response…"
  },
  "Install": {
    "eyebrow": "Install",
    "title": "One command, then scan a QR.",
    "sub": "No accounts, no sign-up. Add the plugin to Pi, pair your phone once, and you're driving every agent from your pocket.",
    "tabFresh": "No Pi yet",
    "tabHasPi": "Already have Pi",
    "labelFresh": "bash — fresh machine",
    "labelHasPi": "bash + Pi",
    "noteFresh": "Installs <b>Pi</b>, the Remote Pi plugin, and the always-on supervisor — then prints the pairing step. No sudo; everything lands in your home directory.",
    "noteHasPi": "Run the first line in your shell; the <code>/remote-pi</code> lines run inside <b>Pi</b>. The first <code>/remote-pi</code> is a quick setup wizard (name + relay), then <b>pair</b> shows a QR you scan with the app.",
    "copy": "Copy",
    "copied": "Copied"
  },
  "Pillars": {
    "tag1": "01 / gateway",
    "title1": "Drive any agent from your phone.",
    "proof1": "Pair with a QR; send prompts, voice, and images; switch models from anywhere.",
    "link1": "How pairing works",
    "tag2": "02 / daemon",
    "title2": "Run as many agents as you want, 24/7.",
    "proof2": "Promote any folder to a background daemon; survives logout, answers at 3am.",
    "link2": "Daemon how-to",
    "tag3": "03 / mesh",
    "title3": "Every machine, one network.",
    "proof3": "Agents across PCs and servers join one mesh under a single key, reachable from your phone.",
    "link3": "See the protocol"
  },
  "GetApp": {
    "eyebrow": "Get the app",
    "title": "Pair your phone, drive your agents.",
    "sub": "The authenticator and the remote control. Get it on the App Store or Google Play, grab the Android APK direct, or download the desktop Cockpit.",
    "appStoreTop": "Download on the",
    "appStoreName": "App Store",
    "appStoreSub": "iOS · iPhone & iPad",
    "playTop": "Get it on",
    "playName": "Google Play",
    "playSub": "Android",
    "apkTop": "Direct download",
    "apkName": "Android APK",
    "apkSub": "Signed · with sha256",
    "cockpitTop": "Desktop app",
    "cockpitName": "Cockpit",
    "cockpitSub": "macOS · Windows · Linux"
  },
  "Strip": {
    "voice": "Voice — dictate, no cloud transcription",
    "image": "Image — send a shot to a multimodal agent",
    "openSource": "Open source — MIT licensed",
    "selfHost": "Self-host — run the relay behind a VPN"
  },
  "GithubCTA": {
    "eyebrow": "Open source",
    "title": "Open source, all the way down.",
    "sub": "Active MVP. Read the source, run the protocol, or self-host the relay — it's all on GitHub.",
    "cta": "View on GitHub",
    "star1": "MIT licensed",
    "star2": "Self-hostable relay",
    "star3": "Harness-agnostic protocol"
  },
  "DownloadPage": {
    "eyebrow": "Download",
    "title": "Download Remote Pi",
    "lede": "The desktop Cockpit and the phone app, built straight from CI. Grab the Cockpit to drive your Pi coding agents from your computer, and the Android app to carry them in your pocket.",
    "notPublishedTitle": "Not published yet",
    "notPublishedBody": "These builds haven't been published to the download host yet, so the links below aren't live. The version, size, and checksum are a preview of the layout — check back soon.",
    "whatsNew": "What's new in {version}",
    "download": "Download",
    "unavailable": "Unavailable",
    "cockpitEyebrow": "Desktop · Cockpit",
    "cockpitTitle": "Remote Pi Cockpit",
    "cockpitDesc": "Pair from your Mac, Windows, or Linux machine, watch live sessions, and manage your 24/7 daemons and schedules from one window.",
    "version": "Version {version}",
    "released": "Released {date}",
    "signedNotarized": "Signed & notarized on macOS",
    "macosName": "macOS",
    "macosTagline": "One universal build for Apple Silicon and Intel.",
    "macosStep1": "Open the downloaded <code>.dmg</code>.",
    "macosStep2": "Drag <b>Remote Pi Cockpit</b> into your <b>Applications</b> folder.",
    "macosStep3": "Launch it from Applications or Spotlight.",
    "macosFoot": "The build is signed with a Developer ID and notarized, so it opens without a Gatekeeper prompt.",
    "windowsName": "Windows",
    "windowsTagline": "Installer for Windows 10 and 11 on x64.",
    "windowsWarningTitle": "SmartScreen notice",
    "windowsWarningBody1": "This build isn't code-signed yet, so Windows SmartScreen may warn that the publisher is unknown. To continue:",
    "windowsWarningBody2": "Click <b>More info</b>, then <b>Run anyway</b> — and follow the installer.",
    "linuxName": "Linux",
    "linuxTagline": ".deb and .rpm packages for x86_64 and arm64.",
    "linuxIntro": "Download the package for your architecture, then install it:",
    "linuxDebLabel": "Debian / Ubuntu — .deb",
    "linuxRpmLabel": "Fedora / RHEL — .rpm",
    "linuxFoot": "Swap <code>amd64</code>/<code>x86_64</code> for <code>arm64</code>/<code>aarch64</code> on ARM machines. The app then appears in your applications menu.",
    "cockpitFoot": "Cockpit drives a local Pi install — it doesn't bundle one. The app's onboarding checks for <code>pi</code>, the Remote Pi plugin, and the supervisor, and walks you through anything missing. New here? <link>Start with the setup guide</link>.",
    "androidEyebrow": "Mobile · Android",
    "androidTitle": "Remote Pi — App (Android)",
    "androidDesc": "The phone app — pair once with a QR, then drive your agents from anywhere. Installed straight from an APK, no Play Store needed.",
    "androidDirect": "Direct APK · signed release",
    "androidName": "Android",
    "androidTagline": "One universal APK for phones and tablets.",
    "androidDownloadLabel": "Download RemotePi.apk",
    "androidStep1": "Download <code>RemotePi.apk</code> to your phone.",
    "androidStep2": "Tap the file to start installing.",
    "androidStep3": "If Android blocks it, allow your browser to <b>install unknown apps</b> when prompted, then continue.",
    "androidStep4": "Optional: check the <b>SHA-256</b> above matches the file before installing.",
    "androidFoot": "Prefer a store? Remote Pi is also on <link>Google Play</link>. iOS ships through the App Store.",
    "shaLabel": "SHA-256",
    "copy": "Copy",
    "copied": "Copied"
  },
  "LegalCommon": {
    "eyebrow": "Legal",
    "lastUpdated": "Last updated: {date}"
  },
  "PrivacyPage": {
    "title": "Privacy Policy",
    "metaDescription": "Privacy Policy for Remote Pi — what little data we touch, why we touch it, and your LGPD rights.",
    "subtitle": "Data controller: <b>Flutterando Desenvolvimento de Programas de Computador LTDA</b> (CNPJ 33.637.582/0001-70). Data Protection Officer (DPO): <b>Jacob Moura</b> — <link>{email}</link>.",
    "s1Title": "Who We Are (Data Controller)",
    "s1P1": "Remote Pi is operated by Flutterando Desenvolvimento de Programas de Computador LTDA, a company incorporated in Brazil (CNPJ 33.637.582/0001-70), with offices at Rua Clara Nunes, 198, Maringá/PR, CEP 87.045-650.",
    "s1P2": "For any matter related to this Policy or to the processing of your personal data, you may contact our Data Protection Officer, Jacob Moura, at <link>{email}</link>.",
    "s2Title": "Data We Collect",
    "s2h1": "2.1 Data you provide directly",
    "s2P1": "Remote Pi is designed so that you provide essentially nothing to us on the relay path. There is no account, no email registration, no profile, and no payment information. The pairing flow generates cryptographic keys locally on your devices.",
    "s2P2": "On the device itself, the mobile app stores a list of paired peers (their public keys, a friendly name you choose, and the relay URL) in the platform's secure storage (iOS Keychain / Android Keystore). This information never leaves your device unless you explicitly send it.",
    "s2h2": "2.2 Data processed automatically by the public relay",
    "s2P3": "When you connect to the public relay operated by Flutterando, the relay processes three categories of data:",
    "s2Li1": "<b>Connection metadata</b> — source IP address, connection timestamps, public-key identifier of the connecting peer, room identifiers, and basic transport statistics (bytes in/out, message timing and sizes). This is <b>logged</b> for at most 30 days and used for abuse mitigation and reliable operation of the relay.",
    "s2Li2": "<b>Message payloads</b> forwarded between paired peers. In the current MVP, payloads travel base64-encoded over TLS and <b>are not end-to-end encrypted at the application layer</b>. The relay operator could in principle access plaintext message contents in memory while forwarding. We do <b>not log, persist, or inspect</b> those payloads — we forward them and discard them. See §9 for the full trust model.",
    "s2Li3": "<b>Signed mesh-membership blobs.</b> When you pair a new machine, your Owner key signs a small JSON blob listing which Pi devices belong to that Owner's mesh, and your app uploads it to the relay via <code>POST /mesh/&lt;owner_pk_hash&gt;</code>. The relay verifies the Ed25519 signature and persists the blob (a few KB per Owner) so that new devices restoring the same Owner key can recover their peer list. The blob contains: Owner public key, a version number, the list of Pi public keys you have paired, and a timestamp. It is <b>not encrypted</b> — anyone with access to the relay database can read it. Pairing on your own self-hosted relay keeps this data on your infrastructure.",
    "s2P4": "If you require cryptographic confidentiality from the relay operator, self-host the relay (the code is open source and documented). When you self-host, the data described in §2.2 is processed by your own infrastructure, not by Flutterando.",
    "s2h3": "2.3 Data we do NOT collect",
    "s2Li4": "Precise device location.",
    "s2Li5": "Contacts, photos, microphone, or camera content.",
    "s2Li6": "The text of your prompts or the responses produced by your Pi-side agent.",
    "s2Li7": "Advertising identifiers (IDFA, AAID).",
    "s2Li8": "Behavioral analytics or tracking telemetry.",
    "s3Title": "How We Use Your Data",
    "s3P1": "The limited connection metadata described in §2.2 is used to:",
    "s3Li1": "Operate, maintain, and route traffic on the relay service.",
    "s3Li2": "Detect and mitigate abuse, such as denial-of-service attacks or patterns of unauthorized access attempts.",
    "s3Li3": "Investigate incidents and protect the security of the Service and its users.",
    "s3P2": "We do not use any data for advertising, profiling, or behavioral analytics.",
    "s4Title": "Legal Bases (LGPD Article 7)",
    "s4P1": "Under the Brazilian General Data Protection Law (Lei Geral de Proteção de Dados, Law 13.709/2018, \"LGPD\"), we process the limited data described above on the following legal bases:",
    "s4Li1": "<b>Performance of a contract</b> (Article 7, V) — to provide the relay service you connect to.",
    "s4Li2": "<b>Legitimate interest</b> (Article 7, IX) — to ensure the security and integrity of the relay infrastructure.",
    "s5Title": "Data Sharing",
    "s5P1": "We do not sell or rent your data. We do not share connection metadata with third parties for advertising or analytics purposes. We may disclose data only when required to do so by a valid legal order under Brazilian law, and only to the extent strictly necessary to comply with that order.",
    "s5P2": "If you choose to self-host your own relay or connect to a relay operated by a third party, that operator becomes the data controller for the connection metadata they process. This Policy does not cover third-party relays.",
    "s6Title": "International Transfer",
    "s6P1": "The public relay operated by Flutterando may be hosted in data centers located outside Brazil. Where this is the case, transfers occur under conditions equivalent to those required by Article 33 of the LGPD, including contractual safeguards with infrastructure providers. You can avoid international transfer entirely by running your own relay on infrastructure under your control.",
    "s7Title": "Data Retention",
    "s7P1": "Relay connection logs are retained for a maximum of <b>30 days</b>, after which they are deleted or anonymized. Aggregated, non-identifying statistics (e.g. daily active connection counts) may be retained longer for capacity planning.",
    "s7P2": "Paired peers stored on your device persist until you revoke the pairing or uninstall the app. We do not have access to that storage.",
    "s8Title": "Your Rights (LGPD Article 18)",
    "s8P1": "Subject to the LGPD, you have the right to request, with respect to personal data we hold about you:",
    "s8Li1": "Confirmation that we process your data.",
    "s8Li2": "Access to that data.",
    "s8Li3": "Correction of incomplete, inaccurate, or outdated data.",
    "s8Li4": "Anonymization, blocking, or deletion of unnecessary or excessive data, or data processed in non-compliance with the LGPD.",
    "s8Li5": "Information about public and private entities with which we have shared your data.",
    "s8Li6": "Information about the possibility of not providing consent, and the consequences of refusal.",
    "s8Li7": "Revocation of consent, where consent was the legal basis.",
    "s8P2": "To exercise any of these rights, contact our DPO at <link>{email}</link>. We may need to verify your identity (for example, by asking you to prove control of a paired device's public key) before fulfilling the request.",
    "s9Title": "Security and trust model",
    "s9P1": "We use the following safeguards:",
    "s9Li1": "<b>TLS 1.3</b> on every connection between clients and the relay.",
    "s9Li2": "<b>Ed25519 challenge-response</b> at pairing time, so paired devices verify each other's identity cryptographically and identity squatting is prevented.",
    "s9Li3": "Private keys generated on-device and stored in the platform secure storage (iOS Keychain / Android Keystore). Private keys never leave your devices.",
    "s9Li4": "Operational separation between transport metadata and any other system, with strict access controls on relay logs.",
    "s9P2": "<b>Important — read this if confidentiality matters to you.</b> Application-layer <b>end-to-end encryption of message payloads is not active in the current MVP</b>. Payloads travel base64-encoded over TLS to the relay and from the relay to the paired device. The public relay operator (Flutterando) could in principle access plaintext message contents in memory while forwarding, but we do <b>not log, persist, or inspect</b> payloads. Per-message end-to-end encryption was removed for MVP stability and is on the roadmap for a future release.",
    "s9P3": "If you require cryptographic confidentiality from the relay operator, <b>run your own relay</b>. The relay is open source and the documentation covers Docker deployment and VPN gating (Tailscale, WireGuard) so that only your devices can reach the relay's WebSocket port at all.",
    "s9P4": "No system is perfectly secure. If you believe your account or device has been compromised, revoke the affected pairing immediately and report the incident to <link>{email}</link>.",
    "s10Title": "Children and Minors",
    "s10P1": "The Service is not directed at, and is not intended for use by, individuals under the age of 13. We do not knowingly collect personal data from minors. If we become aware that we have collected personal data from a minor under 13, we will delete that data promptly.",
    "s11Title": "Cookies",
    "s11P1": "This site does not use tracking, advertising, or analytics cookies. The mobile application and the Pi-side extension do not use cookies either. We may use strictly functional cookies on this site only if needed for security (for example, CSRF protection on a future contact form); none are used today.",
    "s12Title": "Policy Updates",
    "s12P1": "We may update this Policy from time to time. The current version is always published on this site, with the \"Last updated\" date at the top. Material changes will additionally be announced in the project README.",
    "s13Title": "Contact",
    "s13P1": "For questions, requests under the LGPD, or any other privacy matter, contact our DPO, Jacob Moura, at <link>{email}</link>.",
    "s13P2": "You also have the right to lodge a complaint with the Brazilian National Data Protection Authority (Autoridade Nacional de Proteção de Dados — ANPD)."
  },
  "TermsPage": {
    "title": "Terms of Service",
    "metaDescription": "Terms of Service for Remote Pi — provided by Flutterando Desenvolvimento de Programas de Computador LTDA.",
    "subtitle": "Provider: <b>Flutterando Desenvolvimento de Programas de Computador LTDA</b> (CNPJ 33.637.582/0001-70). Address: Rua Clara Nunes, 198, Maringá/PR, CEP 87.045-650, Brazil.",
    "s1Title": "Acceptance of Terms",
    "s1P1": "By installing, accessing, or using the Remote Pi mobile application, the Remote Pi Pi-side extension, the Remote Pi relay service, or any other software or service provided under the Remote Pi name (collectively, the \"Service\"), you agree to be bound by these Terms of Service (\"Terms\"). If you do not agree to these Terms, you must not use the Service.",
    "s1P2": "These Terms form a binding agreement between you and Flutterando Desenvolvimento de Programas de Computador LTDA (\"Flutterando\", \"we\", \"us\", or \"our\").",
    "s2Title": "Account & Pairing",
    "s2P1": "Remote Pi does not require you to create an account, register an email address, or provide personally identifiable information to use the Service. Instead, the Service uses an ephemeral, on-demand pairing flow: a QR code generated locally on the Pi-side extension is scanned by the mobile app, establishing a mutually authenticated channel between the two devices (Ed25519 challenge-response). See the Privacy Policy, section 9, for the full trust model — application-layer end-to-end encryption of payloads is on the roadmap, not yet active.",
    "s2P2": "Each pairing produces a cryptographic key pair stored locally on the phone and the Pi. Pairings can be revoked at any time from either device. You are responsible for keeping your devices secure; anyone with physical or remote access to a paired device can use that pairing.",
    "s2P3": "The Service is not directed at, and may not be used by, individuals under 13 years of age.",
    "s3Title": "Features",
    "s3P1": "The Service currently provides the following features:",
    "s3Li1": "Remote control of a Pi-based coding agent (such as Claude Code, Codex, or similar) from a paired mobile device.",
    "s3Li2": "A local multi-agent messaging mesh on the Pi (UDS broker) that lets multiple agents and the mobile peer exchange messages.",
    "s3Li3": "An optional public relay service that forwards encrypted messages between paired devices when they are not on the same local network. You may instead self-host your own relay using the open-source relay code.",
    "s3P2": "Features may be added, modified, or removed at any time. We will make a reasonable effort to communicate breaking changes through the project README and this site.",
    "s4Title": "User-Generated Content",
    "s4P1": "The prompts you send to your Pi-side agent and the responses produced by that agent (\"User Content\") belong to you. Flutterando does not log, persist, or inspect User Content.",
    "s4P2": "For full transparency about the current trust model: in the current MVP, message payloads travel base64-encoded over TLS between your devices and the relay. Application-layer end-to-end encryption is <b>not</b> active yet (it was removed for MVP stability and is on the roadmap). This means the operator of the relay you connect to could in principle access plaintext message contents while forwarding them. Users who require cryptographic confidentiality from the relay operator should self-host the relay (it is open source and the docs cover Docker + VPN deployment). See our Privacy Policy, section 9, for the full description.",
    "s4P3": "You are solely responsible for the User Content you send through the Service and for ensuring that you have the right to send and receive that content under applicable law and any third-party agreements (including the terms of any AI provider whose model the Pi-side agent uses).",
    "s5Title": "Prohibited Conduct",
    "s5P1": "You must not:",
    "s5Li1": "Attempt to break, bypass, or weaken the cryptographic authentication used by the Service (Ed25519 pairing, TLS to the relay), or attempt to impersonate another paired device.",
    "s5Li2": "Attack, overload, or otherwise disrupt the relay infrastructure or any other component of the Service (denial-of-service, abusive connection patterns, etc.).",
    "s5Li3": "Reverse-engineer the Service for the purpose of producing a confusingly similar product or circumventing security features (reverse engineering for interoperability or security research is permitted to the extent allowed by applicable law).",
    "s5Li4": "Use the Service to commit, facilitate, or attempt unlawful acts, or to violate the rights of any third party.",
    "s6Title": "Reporting and Security Issues",
    "s6P1": "Remote Pi does not host or moderate user-to-user content (the relay is oblivious to message contents), so there is no general content moderation process. However, if you discover a security vulnerability in the Service or believe the Service is being abused to attack infrastructure or users, please report the issue to <link>{email}</link>. We will acknowledge security reports and respond as quickly as reasonably possible.",
    "s7Title": "Platform Intellectual Property",
    "s7P1": "The source code for Remote Pi components is released under the MIT license and can be found in the project's public repository. Subject to that license, you may use, copy, modify, and distribute the source code.",
    "s7P2": "The Remote Pi name, logo, and visual identity (including the π symbol mark, the color palette, and the wordmark) are trademarks and trade dress of Flutterando and are not granted to you by the MIT license. You may not use them in a way that suggests endorsement of, or affiliation with, a derivative product without prior written permission.",
    "s8Title": "Availability and Service Modifications",
    "s8P1": "The Service is provided on an \"as is\" and \"as available\" basis. We do not guarantee that the Service will be uninterrupted, error-free, or free from defects, and we may suspend, modify, or discontinue all or part of the Service at any time without prior notice.",
    "s8P2": "The public relay in particular is a free, best-effort service. If you require strong availability guarantees, you should self-host the relay.",
    "s9Title": "Liability Limitation",
    "s9P1": "To the maximum extent permitted by applicable law, Flutterando shall not be liable for any indirect, incidental, special, consequential, or punitive damages, or for any loss of profits, revenue, data, use, goodwill, or other intangible losses, resulting from (i) your use of or inability to use the Service; (ii) any conduct or content of any third party in the Service; (iii) any unauthorized access to, use of, or alteration of your transmissions or content; or (iv) any other matter relating to the Service.",
    "s9P2": "Nothing in these Terms excludes or limits liability that cannot be excluded or limited under applicable law (including consumer protection rights under Brazilian law).",
    "s10Title": "Terms Modifications",
    "s10P1": "We may update these Terms from time to time. The current version is always published on this site, with the \"Last updated\" date at the top. Material changes will additionally be announced in the project README. Your continued use of the Service after a change becomes effective constitutes acceptance of the new Terms.",
    "s11Title": "Termination",
    "s11P1": "You may stop using the Service at any time. To terminate a specific pairing, revoke it from the mobile app or, on the Pi side, run <code>/remote-pi revoke &lt;id&gt;</code>. You may also uninstall the mobile application and the Pi-side extension to fully stop using the Service.",
    "s11P2": "We may suspend or terminate your access to the public relay if you violate these Terms or if your usage threatens the stability or security of the Service.",
    "s12Title": "Applicable Law",
    "s12P1": "These Terms are governed by the laws of the Federative Republic of Brazil. Any dispute arising out of or relating to these Terms or the Service shall be resolved exclusively in the courts of the district of Maringá, State of Paraná, Brazil, except where Brazilian consumer law mandates a different jurisdiction.",
    "s13Title": "Contact",
    "s13P1": "Questions, notices, and requests under these Terms can be addressed to <link>{email}</link>, attention: Jacob Moura, Data Protection Officer, Flutterando Desenvolvimento de Programas de Computador LTDA."
  },
  "CockpitPage": {
    "title": "Cockpit: Just a terminal. Until your agents need more.",
    "metaDescription": "A fast, local, multiplexed terminal where an IDE emerges around your agents: code viewer, diagnostics, git, databases.",
    "eyebrow": "Remote Pi Cockpit",
    "h1": "Just a terminal. Until your agents need more.",
    "lede": "A fast, multiplexed terminal, 100% local with no cloud and no account, where putting an agent to work makes an IDE emerge around it. On your machine.",
    "download": "Download",
    "seeItGrow": "See it grow",
    "heroShotAlt": "Remote Pi Cockpit as a multiplexed terminal: several real shells split across panes, with a workspace sidebar and file tree.",
    "agentsEyebrow": "Agents",
    "agentsTitle": "Agents live in tabs.",
    "agentsDesc": "Any tab can be a live Pi agent: streaming rich markdown, taking images, editing your code while you watch the diff.",
    "agentsShotAlt": "An agent working in a Cockpit tab on a git worktree, with an inline green-and-red diff and new diagnostic issues reported.",
    "ideEyebrow": "The IDE emerges",
    "ideTitle": "Viewer, diagnostics, git. When you need them.",
    "ideDesc": "Syntax-highlighted code in ~190 languages, live diagnostics and formatting from any language server on your PATH (Dart, TypeScript, Python, Go, Rust and more), plus git status and one-click worktrees. The agent edits; you review.",
    "ideShotAlt": "Cockpit's code viewer showing a Dart file with syntax highlighting, styled documentation comments, and a file path breadcrumb.",
    "dbEyebrow": "Databases",
    "dbTitle": "Databases as tabs.",
    "dbDesc": "SQLite, Postgres, MySQL, SQL Server, Redis and MongoDB: open them as tabs, query them, and let agents do the same through <code>cockpit db</code>.",
    "dbShotAlt": "Cockpit's Database panel listing Postgres, MySQL, SQL Server, Redis, MongoDB, and an auto-detected SQLite connection.",
    "cliEyebrow": "An agentic tmux",
    "cliTitle": "Agents drive the cockpit.",
    "cliDesc": "A built-in <code>cockpit</code> CLI, available only inside its terminals, lets an agent open tabs, type into them, read their output, run your project's tasks, and query your databases.",
    "cliTerminalLabel": "Cockpit terminal",
    "cliReferenceNote": "Every command, flag, and id convention lives in the <link>Cockpit reference</link>.",
    "workspacesEyebrow": "Workspaces & realms",
    "workspacesTitle": "Every context, one click away.",
    "workspacesDesc": "Group projects into workspaces, workspaces into realms, and fork onto a fresh git worktree with your whole layout recreated.",
    "layoutsEyebrow": "Committed setup",
    "layoutsTitle": "Your environment, in the repo.",
    "layoutsDesc": "A <code>.ckp</code> file describes the terminals a project opens — folders, splits, commands — and can apply itself to every new worktree. A <code>.cockpit/tasks.json</code> turns your dev servers into play/stop buttons with profiles and reload on save, on top of the tasks Cockpit already detects from <code>package.json</code> and <code>pubspec.yaml</code>.",
    "layoutsTutorialNote": "Build both from scratch in the <link>layouts and tasks tutorial</link>.",
    "yoursEyebrow": "Make it yours",
    "yoursTitle": "Themes, sounds, your language.",
    "yoursDesc": "Nine built-in themes, or write your own: one JSON file paints the UI, the syntax highlighting, and the terminal palette at once, inheriting everything you don't declare. Pick the terminal font family, bind a sound to each agent event — turn done, action needed, error — and read the whole interface in English, Portuguese, or Spanish.",
    "statusEyebrow": "Turn status",
    "statusTitle": "You always know who's working.",
    "statusDesc": "Cockpit hooks into Claude Code and Codex CLI, so each tab shows whether its agent is working, waiting on you, or done — with a chime when the window is focused and a system notification when it isn't. Nothing to configure: a session started outside Cockpit simply reports nothing.",
    "getEyebrow": "Get Cockpit",
    "getTitle": "Start with a terminal.",
    "getSub": "Free and open source, for macOS, Windows, and Linux — signed, with in-app updates.",
    "reference": "Reference",
    "github": "GitHub",
    "footerNote": "Part of the <link>Remote Pi ecosystem</link>, where daemons, schedules, and the agent mesh live there."
  },
  "WhyPage": {
    "title": "Why Pi",
    "metaDescription": "Deciding how to run an always-on coding agent? remote-pi keeps Pi alive 24/7 and puts it in your pocket. Here's when that's the right shape — and when an all-in-one platform isn't.",
    "eyebrow": "Why Pi",
    "h1": "An always-on agent you assemble yourself.",
    "lede": "remote-pi turns Pi into a background agent that never logs off — and a phone in your pocket that drives it. This page is about that choice: keeping a coding agent alive 24/7, and whether building it up from something small is the shape you want.",
    "gotEyebrow": "What you get",
    "gotTitle": "Pi, kept alive and put in your pocket.",
    "h1Title": "Alive 24/7",
    "h1Desc": "A per-machine supervisor (launchd on macOS, systemd on Linux) keeps every paired folder running as a background agent — survives logout, restarts on crash, answers at 3am.",
    "h2Title": "Lightweight",
    "h2Desc": "Pi is a small coding agent, not a platform. It boots fast and runs only what you add to it — nothing you didn't ask for.",
    "h3Title": "You assemble it",
    "h3Desc": "Extend Pi with the skills, plugins, and per-folder agents you actually need. The agent is yours to shape until it fits your work exactly.",
    "h4Title": "Driven from your phone",
    "h4Desc": "Pair once with a QR. Send prompts, switch models, start a fresh session, or compact context from iOS or Android — wherever you are.",
    "h5Title": "A mesh when you need it",
    "h5Desc": "Agents reach each other on one machine over a local socket, or across PCs through the relay. One Owner key, one mesh, no central server.",
    "h6Title": "Open source, self-hostable",
    "h6Desc": "MIT licensed. Run the community relay or host your own; traffic is encrypted in transit, and self-hosting keeps it on infrastructure you control.",
    "honestEyebrow": "The honest version",
    "honestTitle": "What about OpenClaw and Hermes Agent?",
    "honestP1": "They're excellent. <b>OpenClaw</b> and <b>Hermes Agent</b> are first-class always-on, open-source agents. If you want a batteries-included platform that ships ready to run, you should look hard at them — this page won't pretend otherwise.",
    "honestP2": "remote-pi makes a different bet. It starts from Pi — a lightweight coding agent — and adds just the always-on layer: a supervisor that keeps it running and a phone that drives it. Everything else, you assemble. The trade is real: less out of the box, more that's exactly yours.",
    "choiceTitle": "The choice",
    "choiceBody": "Want a complete, all-in-one platform, ready out of the box? OpenClaw and Hermes Agent are great places to start. Want a lightweight coding agent you assemble, keep alive 24/7, and control from your phone? That's Pi with remote-pi.",
    "scopeNote": "One note on scope: this comparison is about the <i>always-on layer</i> — remote-pi's daemon mode — not coding agents in general. It's the part where keeping an agent alive and reachable is the whole job, and where OpenClaw and Hermes Agent shine too.",
    "getStartedEyebrow": "Get started",
    "getStartedTitle": "Build yours and leave it running.",
    "getStartedSub": "Add the plugin to Pi, pair your phone, and promote a folder to a 24/7 daemon. The how-to walks every step.",
    "install": "Install",
    "daemonHowTo": "Daemon how-to →",
    "github": "GitHub"
  },
  "DocsShared": {
    "pagerAriaLabel": "Tutorial navigation",
    "pagerPrev": "Previous",
    "pagerNext": "Next",
    "calloutNote": "Note",
    "calloutWarning": "Warning",
    "tocOnThisPage": "On this page",
    "installTabsAriaLabel": "Install Remote Pi",
    "installTabsFreshTab": "No Pi yet",
    "installTabsHasPiTab": "Already have Pi",
    "installTabsFreshLabel": "bash — one command",
    "installTabsHasPiLabel": "pi — three commands",
    "installTabsFreshNote": "Installs Pi, the Remote Pi plugin, and the always-on supervisor, then prints the pairing step. No sudo — everything lands in your home directory.",
    "installTabsHasPiNote": "Run them in order: install the plugin, run the setup wizard, then show the pairing QR. Each is explained below.",
    "installTabsSoon": "soon",
    "tutorialOf": "Tutorial · {n} of {total}"
  },
  "TutorialsIndex": {
    "title": "Tutorials",
    "metaDescription": "Hands-on guides for Remote Pi: get started with the app, run a local mesh, route across PCs, and keep an agent alive 24/7.",
    "eyebrow": "Tutorials",
    "h1": "Learn Remote Pi by doing.",
    "lede": "Four hands-on guides, in order. Start with the app, then add agents, cross-PC routing, and a 24/7 supervisor as you need them. For the <em>why</em> behind it, <why>read Why Pi</why>; for exact flags and config, the <docs>reference docs</docs>.",
    "extras": "Extras",
    "openTutorial": "Open tutorial",
    "gettingStartedTitle": "Getting started",
    "gettingStartedDesc": "Install Remote Pi, pair your phone, and drive your first agent from the app.",
    "meshLocalTitle": "Local mesh",
    "meshLocalDesc": "Let two agents on the same machine discover each other and trade messages.",
    "meshRemoteTitle": "Remote mesh",
    "meshRemoteDesc": "Route messages between agents on different PCs through the relay.",
    "daemonTitle": "Daemon mode",
    "daemonDesc": "Keep an agent alive 24/7 with the supervisor, then manage the fleet.",
    "cockpitLayoutsTitle": "Layouts and tasks in Cockpit",
    "cockpitLayoutsDesc": "Commit a .ckp layout that opens your terminals and a tasks.json that runs your dev servers — with profiles and reload on save.",
    "cockpitTeamTitle": "An agent team in Cockpit",
    "cockpitTeamDesc": "Run an orchestrator, backend, and frontend as three panes in Cockpit — each its own folder and AGENTS.md, talking over the remote-pi mesh.",
    "claudeMeshTitle": "Claude in the mesh",
    "claudeMeshDesc": "Put Claude Code on the agent mesh next to Pi — advanced, terminal-only (not in the app yet)."
  },
  "TutorialMeshRemote": {
    "title": "Remote mesh",
    "metaDescription": "Route messages between agents on different PCs through the relay, address peers as pc_label:peer, and understand what an ACK does and does not guarantee.",
    "h1": "Remote mesh",
    "lede": "The <meshLocal>local mesh</meshLocal> stops at one machine. Turn on the relay and the same <code>agent_send</code> reaches agents on other PCs — your laptop talking to your desktop, a build box, a server. The tools are identical; only the addressing changes.",
    "s1Title": "1. Put both machines on one mesh",
    "s1P1": "Cross-PC routing rides the relay. Every machine paired under the same <b>Owner key</b> forms one logical mesh — there is no central server, just a relay forwarding between paired peers. To get there:",
    "s1Li1": "On each machine, run <code>/remote-pi</code> and answer <b>Yes</b> to using the relay.",
    "s1Li2": "Pair both machines to the <b>same Owner</b> so they share one mesh. The mesh-membership details — how the Owner key signs the device list — live in <link>PROTOCOL.md</link>.",
    "s1P2": "Point both machines at the same relay. The default is the community relay; for your own, see <link>the relay reference</link>.",
    "s2Title": "2. Address peers across PCs",
    "s2P1": "Once both machines are on the mesh, <code>list_peers()</code> shows local agents plus remote ones, each prefixed with the source machine's label:",
    "s2CodeLabel1": "any agent · tool call",
    "s2P2": "To message a remote peer, use its full <code>pc_label:peer</code> name verbatim — that's the only difference from a local send:",
    "s2CodeLabel2": "frontend · tool call",
    "s2P3": "The reply arrives the same way a local one does — in your inbox on a future turn, carrying <code>re='<your-send-id>'</code>.",
    "s3Title": "3. \"Delivered\" is not \"alive\"",
    "s3P1": "This is the one thing to internalize about remote sends. A <code>Delivered</code> ACK means <b>the remote broker accepted the envelope</b> — nothing more. It does <em>not</em> mean the peer is alive, processed the message, or will ever answer.",
    "s3CalloutTitle": "Validate by roundtrip",
    "s3CalloutBody": "Don't treat <code>Delivered</code> as proof the other agent is working. <code>list_peers</code> can even list a peer that has gone stale. The only real confirmation is a <b>reply you actually receive</b>: ask the remote agent to respond, then wait for that envelope in your inbox. If it never comes, the peer may be offline regardless of what the ACK said.",
    "s3P2": "When a forwarded message genuinely can't be delivered, the relay returns a <code>transport_error</code> envelope with <code>re='<your-send-id>'</code> — watch your inbox for that too.",
    "s4Title": "What the relay sees",
    "s4P1": "The relay is the only network-touching piece, so it's worth being precise about what it protects and what it doesn't:",
    "s4CalloutTitle": "Encrypted in transit — payloads are not",
    "s4CalloutBody": "Connections to the relay are <b>encrypted in transit (TLS)</b>, and devices authenticate each other with <b>Ed25519 pairing</b>. But message payloads are <b>not</b> encrypted at the application layer — the relay operator could in principle read plaintext in memory while forwarding. If you need confidentiality from the relay operator, <link>self-host the relay</link> behind a VPN.",
    "s4P2": "The community relay does not log, persist, or inspect payloads, but self-hosting is the way to get cryptographic-grade isolation. The full trust model is in <link>PROTOCOL.md</link>.",
    "s5Title": "What it's for",
    "s5P1": "Cross-PC messaging lets you split work across roles that live on different machines — a <code>frontend</code> on your laptop handing tasks to a <code>backend</code> on a beefier box, or a <code>tests</code> agent on a build server reporting back. Same tools, same envelope; the relay just carries it between machines.",
    "s5SubTitle": "Keep them reachable",
    "s5SubP1": "Cross-PC coordination is most useful when the remote agents are always up. That's the next guide: <link>run them as 24/7 daemons</link>."
  },
  "TutorialMeshLocal": {
    "title": "Local mesh",
    "metaDescription": "Run two Pi agents on the same machine and have them discover each other and exchange messages over the local broker with list_peers, agent_send, and get_messages.",
    "h1": "Local mesh",
    "lede": "Two agents running side by side on one machine can see each other and trade messages — no network involved. Here you'll start a second agent, have the first one find it, and watch a message go from one to the other and back.",
    "s1Title": "How local discovery works",
    "s1P1": "Every agent that runs <code>/remote-pi</code> on a machine joins the same local session, named <code>local</code>. They meet through a Unix domain socket — a local broker at <code>~/.pi/remote/sessions/local/broker.sock</code> — so messages never leave the box. The first agent to start hosts the broker; the rest connect to it. If the host exits, another agent takes over automatically.",
    "s1CalloutTitle": "One agent per folder",
    "s1CalloutBody": "A lock allows exactly one Pi agent per directory. To run two agents at once, put them in <b>two different folders</b> — each gets its own workspace, both meet in the <code>local</code> session.",
    "s2Title": "1. Start a second agent",
    "s2P1": "You already have one agent from the <link>getting started</link> guide. Open a second terminal in a <b>different</b> folder and start Remote Pi there too:",
    "s2CodeLabel": "Second terminal",
    "s2P2": "Say the first agent is named <code>agent-a</code> and the second <code>agent-b</code> (the wizard defaults to the folder name). Both are now in the <code>local</code> session.",
    "s3Title": "2. The three mesh tools",
    "s3P1": "The agent-network skill gives the LLM three tools. You don't call them by hand — you ask the agent in plain language and it calls them for you.",
    "s3Li1": "<code>list_peers()</code> — who is online right now. Returns the peer names, one per line.",
    "s3Li2": "<code>agent_send('{ to, body, re? }')</code> — send a message and wait for an ACK. Set <code>re</code> when you're replying to an earlier message.",
    "s3Li3": "<code>get_messages()</code> — drain this agent's inbox. The agent checks it at the start of each turn, so incoming messages surface on its next reply.",
    "s4Title": "3. Send a message across",
    "s4P1": "In <code>agent-a</code>'s terminal, ask it to find its peers:",
    "s4PromptLabel1": "agent-a · prompt",
    "s4P2": "The LLM calls the tool and gets back the roster:",
    "s4ToolLabel1": "agent-a · tool call",
    "s4P3": "Now have it send something:",
    "s4P4": "<code>Delivered</code> means <code>agent-b</code>'s inbox accepted the envelope. On its next turn, <code>agent-b</code> sees it. Ask it:",
    "s4PromptLabel2": "agent-b · prompt",
    "s4ToolLabel2": "agent-b · tool calls",
    "s4P5": "Back in <code>agent-a</code>, the <code>pong</code> shows up on its next turn, carrying <code>re=ab12cd34</code> so the agent knows which message it answers. The whole exchange is event-driven — nothing blocks waiting for a reply.",
    "s5Title": "Reading the ACK",
    "s5P1": "<code>agent_send</code> tells you what happened to the envelope:",
    "s5Li1": "<code>Delivered</code> — the peer's inbox accepted it.",
    "s5Li2": "<code>busy</code> — the peer is mid-turn; the message was dropped. Retry shortly.",
    "s5Li3": "<code>denied</code> — the peer refused it. Don't retry.",
    "s5Li4": "<b>No ACK (timeout)</b> — the peer may be offline.",
    "s5CalloutTitle": "A send is not a request",
    "s5CalloutBody": "<code>agent_send</code> is fire-and-ACK, not RPC. There is no obligation for the other agent to reply. If you need an answer, the prompt to the receiver has to ask for one — like <code>reply to the sender</code> above."
  },
  "TutorialDaemon": {
    "title": "Daemon mode",
    "metaDescription": "Keep a Pi agent alive 24/7: install the supervisor (launchd / systemd --user), register a folder as a daemon, and manage the fleet from one CLI.",
    "h1": "Daemon mode",
    "lede": "So far your agents only run while a terminal is open. Daemon mode keeps a folder running as a background agent that survives logout, restarts on crash, and answers your phone at 3am. This is the <em>how</em>; for the <em>why</em> — and how it compares to all-in-one platforms — see <why>Why Pi</why>.",
    "s1Title": "The shape of it",
    "s1P1": "One <b>supervisor</b> runs per machine. Under it sit N background agents — one per folder you promote. The supervisor is a normal user service: <code>launchd</code> on macOS, <code>systemd --user</code> on Linux. It starts at login, survives reboots, and respawns any agent that crashes.",
    "s1CalloutTitle": "Lock down tool permissions first",
    "s1CalloutBody": "A daemon inherits the same Pi tool permissions your interactive session has — <code>Bash</code>, <code>Edit</code>, <code>Write</code> all run <b>without a prompt</b>, because no one is at the keyboard to approve them. Configure Pi's tool permissions to taste <em>before</em> you promote a folder to a 24/7 daemon. A tool-approval gate is on the roadmap.",
    "s2Title": "1. Install the supervisor (once per machine)",
    "s2P1": "From inside Pi:",
    "s2CodeLabel1": "In Pi",
    "s2P2": "That single command does two things:",
    "s2Li1": "Installs and activates the user-level supervisor service (<code>launchd</code> / <code>systemd --user</code>), so it auto-starts at login and after reboot.",
    "s2Li2": "Symlinks the <code>remote-pi</code> and <code>pi-supervisord</code> CLIs into <code>~/.local/bin/</code> so you can manage daemons from any shell. If that directory isn't on your <code>$PATH</code>, the command prints the line to add.",
    "s2P3": "This is a separate, explicit opt-in — it is <b>not</b> part of the regular setup wizard. You only run it on machines where you want 24/7 agents.",
    "s3Title": "2. Promote a folder to a daemon",
    "s3P1": "No per-folder setup is needed first — <code>create</code> registers any folder and the supervisor injects the daemon's config at spawn (a fixed <code>assistent</code> workspace, relay on), so the folder needs no <code>.pi/remote-pi/</code> of its own. To reach the daemon from your phone, just make sure this machine has been paired once — pairing is per-machine, so any earlier <code>/remote-pi pair</code> on it counts. Then register:",
    "s3CodeLabel": "Shell",
    "s3P2": "The id is a stable hash of the folder path (<code>sha256(realpath)[:8]</code>), so it survives moves and is the same on every machine. With the supervisor running, <code>create</code> <b>starts the daemon right away</b> — there is no separate start step. It restarts on crash and comes back after a reboot on its own.",
    "s3CalloutTitle": "One daemon per folder",
    "s3CalloutBody": "The by-path id rejects a second daemon in the same directory at <code>create</code> time. Pairing stays interactive — a daemon reuses the keypair and paired devices already set up on this machine; it doesn't show a QR itself.",
    "s4Title": "3. Manage the fleet",
    "s4P1": "Every command works as a Pi slash command (<code>/remote-pi …</code>) and, once the CLI is linked, as a plain shell command (<code>remote-pi …</code>):",
    "s4CodeLabel1": "Fleet commands",
    "s4P2": "A daemon receives a prompt as if a user typed it; its response flows back through the same mesh and relay you configured — your phone sees it live, and other agents on the machine see it over the local mesh.",
    "s4SubTitle": "Where the logs are",
    "s4CodeLabel2": "Logs",
    "s4SubP1": "Each daemon's output is forwarded into the supervisor log with a <code>['<cwd>']</code> prefix, so one stream shows the whole fleet.",
    "s5Title": "4. Schedule recurring prompts (cron)",
    "s5P1": "A daemon only acts when something prompts it. <code>cron</code> lets the supervisor be that something on a schedule — “every weekday at 9am, summarize the new PRs” — with no one at the keyboard. Jobs target a daemon by id and survive reboots along with the supervisor.",
    "s5CalloutTitle": "Cron needs the supervisor installed as a service",
    "s5CalloutBody": "The scheduler runs <i>inside</i> the supervisor, so it only fires when the supervisor is installed as a user service. Run <code>/remote-pi install</code> (step 1) first — without it, <code>cron add</code> warns instead of pretending to schedule. It is the same launchd / systemd service that keeps your daemons alive; there is no second scheduler.",
    "s5P2": "With a daemon registered (step 2) and the supervisor running, add a job. The first argument is the daemon id, then a standard five-field cron expression, then the prompt:",
    "s5CodeLabel1": "Schedule a prompt",
    "s5P3": "Runs must be at least <b>60 seconds</b> apart — a more frequent expression is rejected with a clear message, since every fire spends tokens. Flags on <code>cron add</code>:",
    "s5Li1": "<code>--tz Area/City</code> — run in a specific, DST-aware timezone (e.g. <code>America/Sao_Paulo</code>). Defaults to the machine's local time.",
    "s5Li2": "<code>--wake</code> — if the daemon is stopped when the job fires, start it first, then send. Default: skip (logged as <code>skipped_down</code>).",
    "s5Li3": "<code>--no-skip-busy</code> — send even if the daemon is mid-turn. By default a fire is skipped while the daemon is still working (<code>skipped_busy</code>) so prompts don't pile up on an unfinished turn.",
    "s5Li4": "<code>--catchup</code> — after the supervisor was down, fire <i>one</i> missed run on startup. Default off; never replays the whole backlog.",
    "s5P4": "Each job gets an id like <code>j_ab12</code> (printed by <code>cron add</code> and <code>cron list</code>). Inspect, test, and audit the fleet's schedule:",
    "s5CodeLabel2": "Inspect & audit",
    "s5P5": "The agent's reply is fire-and-forget into the mesh — your phone and other agents see it live, exactly like a manual <code>daemon send</code>. Cron itself only audits the <i>trigger</i>: every fire and every skip appends one line to <code>~/.pi/remote/cron.jsonl</code>, which <code>cron log</code> tails. Full subcommand reference is in the <link>docs</link>.",
    "s6Title": "Removing a daemon",
    "s6CodeLabel": "Cleanup",
    "s6P1": "<code>uninstall</code> is reversible — re-running <code>/remote-pi install</code> later brings every registered daemon back. Full flags and paths are in the <link>reference docs</link>."
  },
  "TutorialGettingStarted": {
    "title": "Getting started",
    "metaDescription": "Install Remote Pi on Pi, pair your phone, and send your first command to an agent from the app — from install to first prompt.",
    "h1": "Getting started",
    "lede": "By the end of this guide you'll have Remote Pi running on one machine, your phone paired to it, and your first command sent from the app — the agent runs it and the result streams back to your pocket. Plan on about five minutes.",
    "s1Title": "Before you start",
    "s1P1": "You need two things:",
    "s1Li1": "A machine with <b>Pi</b> (the coding agent) and <b>Node 20+</b> installed.",
    "s1Li2": "The <b>Remote Pi</b> app on your phone — from the <appStore>App Store</appStore>, <googlePlay>Google Play</googlePlay>, or the <apk>Android APK</apk> direct download.",
    "s1P2": "No Pi yet? The <b>No Pi yet</b> tab below runs a one-command <code>curl</code> installer that sets up Pi, the plugin, and the supervisor for you. Already have Pi? Use the <b>Already have Pi</b> path.",
    "s2Title": "1. Install the plugin",
    "s2P1": "Remote Pi is a Pi plugin. Add it, run the setup wizard, then show a pairing QR:",
    "s2P2": "Walking through the three commands:",
    "s2Sub1P1": "Installs the plugin into Pi. This registers the <code>/remote-pi</code> slash command and deploys the agent-network skill that teaches the LLM the mesh tools.",
    "s2Sub2P1": "The first run opens a short wizard that <b>creates the config for this folder</b>. It asks two questions:",
    "s2Sub2Li1": "<b>Agent name</b> — how other peers address this agent. Defaults to the folder name.",
    "s2Sub2Li2": "<b>Use the relay?</b> — answer <code>Yes</code> so your phone (and, later, other PCs) can reach this agent.",
    "s2Sub2P2": "When it finishes, the agent has joined the local mesh and the relay is connected.",
    "s2Sub3P1": "Prints a QR code (and a copy-paste pairing URI). Leave it on screen for the next step. Pairing is <b>per machine</b>: once a phone is paired, every Pi agent on this machine accepts it.",
    "s2CalloutTitle": "Order matters",
    "s2CalloutBody": "Run <code>/remote-pi</code> before <code>/remote-pi pair</code>. On a brand-new folder with no config, <code>pair</code> will tell you to run the wizard first — that's the config step doing its job.",
    "s3Title": "2. Pair your phone",
    "s3P1": "With the QR on screen:",
    "s3Li1": "Open the Remote Pi app on your phone.",
    "s3Li2": "Tap <b>Pair a device</b> (or the scan button) and point the camera at the QR.",
    "s3Li3": "The app confirms the pairing and the agent shows up in your device list. The Pi terminal footer flips to <code>🟢 relay</code> and shows <code>📱 '<shortid>'</code> while the phone is connected.",
    "s3P2": "Pairing authenticates the two sides to each other with Ed25519, and all relay traffic is encrypted in transit (TLS).",
    "s4Title": "3. Send your first command",
    "s4P1": "You're now driving the agent from your phone. In the app's chat for this agent, type a prompt and send it:",
    "s4CodeLabel": "From the app",
    "s4P2": "The prompt lands in the Pi session on your machine exactly as if you had typed it there. The agent runs, and its response streams back to your phone live. That round trip — phone to agent and back — is the whole point of Remote Pi.",
    "s4P3": "Beyond chatting, the app can drive the session with a few typed actions: <b>compact context</b>, <b>new session</b>, <b>set model</b>, and <b>set thinking</b> level. The model picker reads live from the host, so it always reflects what that machine can actually run.",
    "s5Title": "Where to go next",
    "s5P1": "That's one agent, one phone. From here you can let multiple agents talk to each other, reach across machines, or keep an agent running when you walk away:",
    "s5Li1": "<link>Local mesh</link> — two agents on the same machine discovering and messaging each other.",
    "s5Li2": "<link>Daemon mode</link> — promote a folder to a 24/7 background agent."
  },
  "TutorialClaudeMesh": {
    "title": "Claude in the mesh",
    "metaDescription": "Advanced extra: remote-pi claude puts Claude Code on the agent mesh as a named peer next to Pi — agent-to-agent, driven from the terminal. Not in the app yet.",
    "badge": "Extra · terminal only — no app yet",
    "eyebrow": "Tutorial · Extra",
    "h1": "Claude in the mesh",
    "lede": "<code>remote-pi claude</code> puts <b>Claude Code</b> on the same agent mesh as Pi — a named peer on the local UDS broker and, through the relay, across PCs. This is an advanced, agent-to-agent setup driven entirely from the terminal. It is <b>not surfaced in the mobile app yet</b> — see <why>why</why> at the end.",
    "s1Title": "Before you start",
    "s1Li1": "The <code>remote-pi</code> CLI on your <code>$PATH</code> — run <code>/remote-pi install</code> once (see the <link>daemon tutorial</link>) to link it.",
    "s1Li2": "<b>Claude Code</b> installed and on your <code>$PATH</code> as <code>claude</code>.",
    "s1Li3": "A folder that <b>isn't</b> already running a Pi agent (see <link>one agent per folder</link>).",
    "s2Title": "Run it",
    "s2CodeLabel1": "Shell",
    "s2P1": "On the first run in a folder, it asks for an agent name (defaulting to the folder name) and saves a small config:",
    "s2CodeLabel2": "First run",
    "s2P2": "That name is how other agents address this Claude in <code>list_peers</code> and <code>agent_send</code>. Then it wires three things into Claude Code and launches it.",
    "s3Title": "What it wires in",
    "s3P1": "<code>remote-pi claude</code> is a wrapper. It injects three things, then spawns <code>claude</code> in the target folder.",
    "s3Sub1Title": "1. An MCP server (the mesh tools)",
    "s3Sub1P1": "It registers a stdio MCP server named <code>remote-pi-mesh</code> in Claude's <b>local scope</b> — per-folder, stored in <code>~/.claude.json</code>, not written into the project directory and not committed to version control:",
    "s3Sub1CodeLabel": "What the wrapper runs (you don't type this)",
    "s3Sub1P2": "The server exposes three tools to Claude — the same mesh API Pi agents get:",
    "s3Sub1Li1": "<code>list_peers</code> — who is online: local peers plus cross-PC ones in <code>pc_label:peer</code> form (e.g. <code>MacMini:backend</code>).",
    "s3Sub1Li2": "<code>agent_send('{ to, body, re? }')</code> — send a message and get an <b>ACK</b> back (<code>received</code>, <code>busy</code>, <code>denied</code>, or <code>timeout</code>). It is <b>not fire-and-forget</b>; set <code>re</code> to the id of the message you're replying to.",
    "s3Sub1Li3": "<code>get_messages</code> — drain this agent's inbox of pending messages.",
    "s3Sub2Title": "2. The agent-network skill",
    "s3Sub2P1": "An MCP server gives Claude the tools but not the <em>habits</em>. Skills load only from disk, so the wrapper deploys one to <code>~/.claude/skills/agent-network/SKILL.md</code>. The skill's description self-gates to mesh contexts, so it doesn't intrude on unrelated Claude sessions. It teaches Claude to:",
    "s3Sub2Li1": "call <code>get_messages</code> at the start of every turn, so incoming messages are seen promptly;",
    "s3Sub2Li2": "read the <code>agent_send</code> ACK and act on it (retry on <code>busy</code>, give up on <code>denied</code>) rather than assuming delivery;",
    "s3Sub2Li3": "reply by echoing the original message id in <code>re</code>, so threads stay linked;",
    "s3Sub2Li4": "treat <code>broadcast</code> as fire-and-forget — no per-recipient ACK.",
    "s3Sub3Title": "3. Channel push (wake on message)",
    "s3Sub3P1": "When a message lands, the MCP server emits a <code>notifications/claude/channel</code>. Whether that reaches Claude immediately depends on one launch flag:",
    "s3Sub3Li1": "<b>Push</b> — with <code>--dangerously-load-development-channels server:remote-pi-mesh</code> on (the wrapper sets it), the notification <b>wakes Claude</b> right away, so it reacts to an incoming message without you prompting it.",
    "s3Sub3Li2": "<b>Poll</b> — without that flag, Claude only sees the message the next time it calls <code>get_messages</code> (i.e. on its next turn).",
    "s4Title": "The --dangerously-* flags",
    "s4P1": "The wrapper launches Claude with two flags:",
    "s4CodeLabel": "Launch (the wrapper runs this)",
    "s4CalloutTitle": "Know what these flags do",
    "s4CalloutBody": "<code>--dangerously-skip-permissions</code> <b>auto-approves every tool call</b> — Claude runs Bash, edits, and writes without prompting you, which is what makes unattended agent-to-agent work possible but also removes your approval gate. <code>--dangerously-load-development-channels</code> opens a development channel for the local MCP server (it shows a one-time confirmation dialog at startup). Only point this at folders and peers you trust — same posture as promoting a folder to a <link>daemon</link>.",
    "s5Title": "One agent per folder",
    "s5P1": "A kernel-enforced lock (the same one <code>/remote-pi</code> takes) allows <b>one remote-pi agent per folder</b> — Pi <em>or</em> Claude, never both in the same directory. If a folder already has a Pi agent, start Claude in a different folder; both still meet in the same local mesh. A second agent in a locked folder is refused before it connects, so it never becomes a ghost peer.",
    "s6Title": "Why this isn't in the app yet",
    "s6P1": "The mobile app talks to the <b>relay</b>, and it sees only the Pi agent that paired it. It does not see local UDS peers — like a Claude joined with <code>remote-pi claude</code> — so a mesh-mate Claude won't show up in your phone. Surfacing the full mesh in the app is future work; for now this is a terminal-driven, agent-to-agent feature. Relay traffic, where it's used, is encrypted in transit.",
    "s6P2": "To watch two agents talk on one machine, pair this with the <link>local mesh</link> tutorial — start one peer as Pi and another as Claude, in two folders, and have them message each other.",
    "allTutorials": "All tutorials"
  },
  "TutorialCockpitLayouts": {
    "title": "Layouts and tasks in Cockpit",
    "metaDescription": "Commit your working setup: a .ckp layout that opens the right terminals in the right folders, and a .cockpit/tasks.json that runs your dev servers with reload on save.",
    "badge": "Cockpit · workflow",
    "eyebrow": "Tutorial · Cockpit",
    "h1": "Layouts and tasks in Cockpit",
    "lede": "Two small files turn “set up my environment” into one click: a <code>.ckp</code> layout that opens the right terminals in the right folders, and a <code>.cockpit/tasks.json</code> that runs your dev servers with buttons, profiles, and reload on save. Both live in the repository, so your teammates — and every new git worktree — get the same setup for free.",
    "s1Title": "What you'll build",
    "s1P1": "A small monorepo with an API and a web app. By the end, opening the project gives you three terminals in the right folders, a task list with both servers ready to play, and a new worktree that recreates the whole thing by itself.",
    "s1CodeLabel": "project layout",
    "s1P2": "Everything here is Cockpit-local: no account, no mesh, no agents required. If you want agents on top of this, follow <link>An agent team in Cockpit</link> next.",
    "s2Title": "Before you start",
    "s2Li1": "<b>Cockpit installed</b> — macOS, Windows, or Linux, from the <link>download page</link>.",
    "s2Li2": "A project folder open as a workspace. Anything with a couple of subfolders works; the commands below assume <code>npm</code>, but the runner is generic.",
    "s3Title": "1. Write your first layout",
    "s3P1": "Create <code>dev.ckp</code> at the root of the project. A layout is YAML, and the smallest useful one is three lines:",
    "s3P2": "Right-click the file in Cockpit's tree and choose <b>Open layout</b>. A tab named <code>Agent</code> opens at the project root and types <code>claude</code> for you. Apply it again: nothing happens. A pane whose <code>name</code> already exists is skipped, so a layout is safe to re-run and never closes anything.",
    "s3CalloutTitle": "The name is the label",
    "s3CalloutBody": "<code>name</code> becomes the tab's <b>stable label</b> — the one the <code>cockpit</code> CLI can address by name instead of by a boot-scoped id, and the same label you would otherwise set by double-clicking the tab.",
    "s4Title": "2. Add splits and folders",
    "s4P1": "Now the real geometry. <code>cwd</code> is relative to the <code>.ckp</code> file (forward slashes only, so the file works on every OS), and <code>split</code> says where each pane is born relative to the one created before it: <code>tab</code> (default), <code>right</code>, or <code>down</code>.",
    "s4P2": "You can also apply it from inside any Cockpit terminal, which is how an agent sets up its own workspace:",
    "s4CodeLabel": "Cockpit terminal",
    "s4CalloutBody": "That one line is the payoff. Every time you fork the workspace onto a fresh git worktree, Cockpit applies the layout by itself — and since a new worktree starts empty, the splits come out exactly as written. Keep a single autorun file at the root: with two or more, none of them runs, because ambiguity is never guessed.",
    "s5Title": "3. Declare your tasks",
    "s5P1": "Cockpit already detects tasks from <code>package.json</code> and <code>pubspec.yaml</code> with no configuration — open the Tasks panel and your scripts are there. You write <code>.cockpit/tasks.json</code> when you want more than that: a monorepo, interactive keys, environment profiles.",
    "s5P2": "The file goes at the root of the workspace you open — discovery is literal, with no walking up the tree. Each task's <code>cwd</code> is relative to that file, which is what lets one file at the monorepo root drive every subpackage. Hit play and the output streams into its own tab, with stop and restart controls and any <code>interactiveKeys</code> you declared as buttons that write straight to the process' stdin.",
    "s5P3": "<code>profiles</code> are launch configs: a chip cycles them before you press play, appending <code>args</code> and merging <code>env</code>. The runner stays generic on purpose — a Flutter flavor or a <code>--dart-define</code> is just more <code>args</code>.",
    "s5CalloutTitle": "Reload on save",
    "s5CalloutBody": "For a process that does not watch files itself (<code>flutter run</code> is the classic case), add a <code>watch</code> block: <code>paths</code> to observe, <code>ignore</code> to keep build folders from looping, and <code>onChange</code> pointing at an <code>interactiveKeys</code> label (<code>\"Hot reload\"</code>) or <code>\"__restart__\"</code>. Skip it for Vite or Next, which already reload on their own.",
    "s6Title": "4. Let an agent use them",
    "s6P1": "Both files are reachable from the internal <code>cockpit</code> CLI, which exists only inside Cockpit's terminals. That means an agent in one tab can start your dev server, read what it printed, and open a file for you to look at — without a screenshot or a screen scrape.",
    "s6CodeLabel": "Cockpit terminal",
    "s6P2": "Task ids are stable per workspace: <code>json:'<label>'</code> for the ones you declared, <code>npm:'<script>'</code> and <code>flutter:run</code> for detected ones. The full command surface is in the <link>Cockpit reference</link>.",
    "s7Title": "5. Commit them",
    "s7P1": "Both files belong in git. <code>dev.ckp</code> is your working geometry, and <code>.cockpit/tasks.json</code> is how the project is run — the same reason a <code>Makefile</code> is committed. A teammate clones, opens the folder in Cockpit, and gets your terminals and your task list. Point <code>$schema</code> at <code>tasks.schema.json</code> from the repository and their editor will autocomplete the fields as well; Cockpit ignores the field when running.",
    "s7P2": "From here: <link1>put a team of agents</link1> into those panes, or read the <link2>Cockpit reference</link2> for every field, flag, and the theme format.",
    "pagerPrevLabel": "Meet Cockpit"
  },
  "TutorialCockpitTeam": {
    "title": "An agent team in Cockpit",
    "metaDescription": "Use Cockpit's multiplexer to run an orchestrator, a backend, and a frontend agent side by side — each in its own folder with its own AGENTS.md, coordinating over the remote-pi mesh.",
    "badge": "Cockpit · multi-agent",
    "eyebrow": "Tutorial · Cockpit",
    "h1": "An agent team in Cockpit",
    "lede": "Cockpit's real power is the multiplexer: many agents in one window, each in its own folder. Here you'll wire up three — an <b>orchestrator</b>, a <b>backend</b>, and a <b>frontend</b> — each with its own <code>AGENTS.md</code>, all talking to each other over the <code>remote-pi</code> mesh.",
    "s1Title": "What you'll build",
    "s1P1": "Three Pi agents, side by side as panes in a single Cockpit workspace. Each lives in its own subfolder and reads that folder's <code>AGENTS.md</code> as its standing brief, so each one boots into a role. They coordinate by sending each other messages on the local mesh — the orchestrator splits a request, hands tasks to the backend and frontend, and stitches their replies back together.",
    "s1P2": "Nothing here leaves your machine: the mesh runs over a local socket, and all three agents work on your real files.",
    "s1CalloutTitle": "Two pieces you already know",
    "s1CalloutBody": "This builds on <link1>Getting started</link1> (install Pi + the <code>remote-pi</code> extension) and <link2>Local mesh</link2> (how two agents trade messages). Skim those first if the mesh tools are new to you.",
    "s2Title": "Before you start",
    "s2Li1": "<b>Cockpit installed</b> — grab it from the <link>Cockpit page</link>. On first launch its onboarding checks for <code>pi</code>, the <code>remote-pi</code> extension, and the supervisor, and helps you install anything missing.",
    "s2Li2": "The <code>remote-pi</code> extension is what hands every agent the mesh tools, so make sure onboarding is green before you build the team.",
    "s3Title": "1. Lay out the folders",
    "s3P1": "An agent's identity comes from its folder, so give each teammate one. In your project, create three subfolders, each with its own <code>AGENTS.md</code>:",
    "s3CodeLabel1": "project layout",
    "s3P2": "<code>AGENTS.md</code> is the brief Pi reads when it starts in a folder — the agent's role and house rules. Give each one a clear job and tell it how to behave on the mesh.",
    "s3CalloutTitle": "One agent per folder",
    "s3CalloutBody": "Pi allows exactly one agent per directory — which is precisely why each teammate gets its own subfolder. Three folders, three agents, three distinct peers on the mesh.",
    "s4Title": "2. Open the three panes",
    "s4P1": "In Cockpit, open <code>my-app/</code> as a workspace. The file tree on the right shows your three subfolders. For each one, <b>right-click the folder</b> and create an agent there — Cockpit roots that agent in the subfolder, so it picks up the right <code>AGENTS.md</code>.",
    "s4P2": "Split the canvas so all three are visible at once — orchestrator on one side, backend and frontend on the other — and drag the dividers to taste. One agent streaming a long answer never freezes the others, and the whole layout comes back exactly like this the next time you open the app.",
    "s4CalloutTitle": "Name them to match",
    "s4CalloutBody": "When an agent asks for a name (or in its <code>/remote-pi</code> wizard), use <code>orchestrator</code>, <code>backend</code>, and <code>frontend</code>. Those are the names teammates address in <code>agent_send</code>, so matching them to the folders keeps the prompts readable.",
    "s5Title": "3. Put them on the mesh",
    "s5P1": "Agents meet in a shared session named <code>local</code>, over a Unix socket — no network. Join each agent the same way as in the terminal: in each pane's composer, run the slash command once.",
    "s5CodeLabel1": "each pane · composer",
    "s5P2": "The first run is a quick wizard (name + relay); accept the folder name so the peer is called <code>orchestrator</code>, <code>backend</code>, or <code>frontend</code>. Then confirm everyone is home — ask the orchestrator:",
    "s5CodeLabel2": "orchestrator · prompt",
    "s5CodeLabel3": "orchestrator · tool call",
    "s5P3": "If a teammate is missing from the roster, run <code>/remote-pi</code> in its pane and check again.",
    "s6Title": "4. Run the orchestration",
    "s6P1": "Now give the orchestrator something real and let it delegate. You only talk to the orchestrator — it talks to the others.",
    "s6CodeLabel1": "orchestrator · prompt",
    "s6P2": "It breaks the work in two and sends a task to each peer. A send returns an ACK, not an answer — it's fire-and-ACK, not a blocking call.",
    "s6CodeLabel2": "orchestrator · tool calls",
    "s6P3": "On its next turn, <code>backend</code> sees the task, does the work <em>in its own folder</em>, and replies — quoting the message id in <code>re</code> so the orchestrator knows which task it answers:",
    "s6CodeLabel3": "backend · tool calls",
    "s6P4": "<code>frontend</code> does the same in its folder. Back in the orchestrator, both replies arrive on its next turns; it reconciles them — for instance, forwarding the exact API shape to the frontend if it asked — and reports the finished feature back to you. Three agents, three folders, one coordinated change.",
    "s6CalloutTitle": "If a reply never comes",
    "s6CalloutBody": "An <code>agent_send</code> to a peer that's mid-turn can come back <code>busy</code> — the message was dropped, so retry shortly. The full set of ACKs (<code>Delivered</code>, <code>busy</code>, <code>denied</code>, timeout) is covered in <link>Local mesh</link>.",
    "s7Title": "Why do this in Cockpit",
    "s7P1": "You could run three terminals — but in Cockpit the whole team lives in one window. Every agent streams its own work in its own pane, you watch the orchestrator hand off and the others pick up in real time, and the layout (and each session's history) comes back when you reopen the app. Add a real terminal pane to run the servers, and the build, the agents, and their conversation are all in front of you at once.",
    "s7P2": "From here: promote the orchestrator to a <link1>24/7 daemon</link1> so the team keeps coordinating in the background, or read <link2>Remote mesh</link2> to add a teammate running on a different machine.",
    "pagerPrevLabel": "Local mesh",
    "pagerNextLabel": "Meet Cockpit"
  },
  "DocsPage": {
    "title": "Docs",
    "metaDescription": "Reference for Remote Pi: the relay, protocol & security, the full command reference, configuration files, and troubleshooting.",
    "eyebrow": "Documentation",
    "h1": "Remote Pi docs",
    "lastUpdated": "Last updated: 2026-05-31",
    "license": "License: MIT",
    "intro": "This is the <b>reference</b>. Remote Pi is a mesh for coding agents: agents on the same machine talk through a local UDS broker, agents on different machines reach each other through an open-source relay, and your phone authenticates new peers and drives sessions. The first supported harness is the <piLink>Pi coding agent</piLink>; <code>/remote-pi</code> wires everything up. To <b>learn by doing</b>, start with the <tutorialsLink>tutorials</tutorialsLink>; for <b>why</b> it works this way, see <whyLink>Why Pi</whyLink>. The pages below are for looking things up.",
    "tocQuickStart": "Quick start",
    "tocWhatItDoes": "What it does",
    "tocInstall": "Install",
    "tocUsingRemotePi": "Using",
    "tocPairing": "Pairing a mobile device",
    "tocQuickActions": "Quick actions from the phone",
    "tocAgentNetwork": "Agent network",
    "tocDaemonMode": "Daemon mode",
    "tocRelay": "The relay",
    "tocCommunityRelay": "Community relay",
    "tocSelfHost": "Self-host",
    "tocPointPi": "Point Pi at your relay",
    "tocProtocol": "Protocol & Security",
    "tocCommands": "Command reference",
    "tocCommandsLocal": "Local session",
    "tocCommandsDaemon": "Daemon fleet",
    "tocCommandsCron": "Daemon cron",
    "tocConfig": "Configuration files",
    "tocTroubleshooting": "Troubleshooting",
    "tocFooterStuck": "Stuck on pairing",
    "tocTimeoutMobile": "Mobile times out",
    "tocTimeoutRequest": "Reply never arrives",
    "tocOnePiPerCwd": "One Pi per cwd",
    "tocLinks": "Links",
    "s1Title": "Quick start",
    "s1P1": "Install the plugin, run the setup wizard, and pair your phone in a few commands — then send your first prompt from the app. The full walkthrough, including the mobile side, is a tutorial.",
    "s1P2": "→ <link>See the Getting started tutorial</link>.",
    "s2Title": "What it does",
    "s2P1": "Remote Pi adds two independent layers on top of Pi. The <b>agent network</b> lets agents discover and message each other — over a local socket on one machine, or through the relay across PCs. The <b>mobile control plane</b> is your phone: it authenticates new peers into the mesh and drives sessions. Each is covered hands-on:",
    "s2Li1": "<link>Local mesh</link> — agents discovering and messaging on the same machine.",
    "s2Li2": "<link>Remote mesh</link> — routing between agents on different PCs.",
    "s2Li3": "<link>Getting started</link> — pairing your phone and driving an agent from it.",
    "s3Title": "Install",
    "s3P1": "Requirements: Node 20+ and Pi (the host coding agent). Remote Pi installs as a Pi plugin with <code>pi install npm:remote-pi</code>, which self-registers the <code>/remote-pi</code> slash command and deploys the agent-network skill. The complete setup — wizard, pairing, first command — is in the tutorial.",
    "s3P2": "→ <link1>See the Getting started tutorial</link1>. Daemon mode has its own one-time install — see <link2>Daemon mode</link2> below.",
    "s4Title": "Using /remote-pi",
    "s4P1": "<code>/remote-pi</code> is the everyday entry point. The first run opens a short wizard (agent name, whether to use the relay) that creates the per-folder config; later runs join the local mesh and start the relay automatically. Re-run the wizard with <code>/remote-pi setup</code>. Every subcommand is in the <link>command reference</link>.",
    "s4P2": "→ <link>See the Getting started tutorial</link> for the guided flow.",
    "s5Title": "Pairing a mobile device",
    "s5P1": "<code>/remote-pi pair</code> prints a QR (and a copy-paste URI); scan it with the Remote Pi app. Pairing is <b>per machine</b> — once a device is paired, every Pi process on that machine accepts it. Manage devices with <code>/remote-pi devices</code> and <code>/remote-pi revoke '<shortid>'</code> (see the <link>command reference</link>).",
    "s5P2": "→ <link>See the Getting started tutorial</link>.",
    "s6Title": "Quick actions from the phone",
    "s6P1": "Beyond chatting, the app drives a session with a small set of typed actions — <b>compact context</b>, <b>new session</b>, <b>set model</b>, and <b>set thinking</b> level. The model picker reads live from the host, so it always reflects what that machine can run. The full vocabulary, wire format, and fallback semantics live in <link>PROTOCOL.md</link> (the <em>App actions</em> section).",
    "s6P2": "→ <link>See the Getting started tutorial</link>.",
    "s7Title": "Agent network",
    "s7P1": "Each agent is a peer in a mesh. The LLM gets three tools — <code>list_peers</code> (who is online), <code>agent_send</code> (send with an ACK), and <code>get_messages</code> (drain the inbox). On one machine, peers talk over a Unix-domain-socket broker; across machines, the same <code>agent_send</code> routes through the relay, addressing remote peers as <code>pc_label:peer</code>. Both paths are covered hands-on:",
    "s7Li1": "<link>Local mesh</link> — the broker, the three tools, a concrete exchange.",
    "s7Li2": "<link>Remote mesh</link> — cross-PC addressing and what an ACK does (and doesn't) guarantee.",
    "s8Title": "Daemon mode",
    "s8P1": "Promote a folder to a 24/7 background agent: run <code>/remote-pi install</code> once per machine to install the supervisor (launchd on macOS, <code>systemd --user</code> on Linux) and link the CLI, then <code>remote-pi create '<folder>' --name \"…\"</code> to register and start a daemon. One supervisor per machine, N daemons under it. Every command is in the <link>command reference</link> below.",
    "s8P2": "→ <link1>See the Daemon mode tutorial</link1> for the full how-to; the <em>why</em> (and how it compares to all-in-one platforms) is <link2>Why Pi</link2>.",
    "s9Title": "The relay",
    "s9P1": "The relay is the only network-touching piece of Remote Pi. In the current MVP it sees both message payloads (forwarded but never logged or inspected by the community operator) and connection metadata: which keypair is online, which room/cwd identifiers exist, message timing, sizes. Traffic is encrypted in transit (TLS) and peers authenticate with Ed25519 pairing, but payloads are not encrypted at the application layer — see <link>Protocol & Security</link> and the Privacy Policy, section 9, for the full picture.",
    "s9P2": "The relay also <b>persists a small SQLite table</b> called <code>mesh_versions</code> — blobs signed by your Owner key listing the Pi devices that belong to your mesh (a few KB per Owner). The relay verifies the Ed25519 signature on every <code>POST /mesh/'<owner_pk_hash>'</code> and stores what you signed; it never decides membership itself. New devices restoring your Owner key recover their peer list from this blob. A relay compromise means DoS, not impersonation. See the <link>PROTOCOL.md</link> mesh-membership section for the wire format.",
    "s9P3": "You have two options.",
    "s9Sub1Title": "Option A — Use the community relay",
    "s9Sub1P1": "<code>https://relay-rp1.jacobmoura.work</code> (default). Zero setup. Good for trying things out or for casual use. (Internally the extension uses the WebSocket form <code>wss://…</code> — both schemes point at the same endpoint.)",
    "s9Sub1P2": "Caveats:",
    "s9Sub1Li1": "Shared infrastructure — availability is best-effort.",
    "s9Sub1Li2": "<b>TLS in transit is the only network protection</b> — the relay operator sees plaintext envelopes (payloads, routing metadata, peer pubkeys, timing). Self-host for confidentiality from the operator.",
    "s9Sub1Li3": "<b>No IP allow-listing or VPN gating</b> built in. Anyone with a paired keypair can connect; layer a VPN on top via Option B if you want network-level isolation.",
    "s9Sub2Title": "Option B — Self-host (recommended for privacy)",
    "s9Sub2P1": "Run the relay yourself in Docker and put it behind a VPN like <tailscaleLink>Tailscale</tailscaleLink>, <wireguardLink>WireGuard</wireguardLink>, or your own VPC. Because the relay's network-level protection is just TLS + keypair authentication, layering a VPN on top means <b>only your devices</b> can even reach the WebSocket port — defense in depth.",
    "s9Sub2P2": "Quick Docker outline (see the <link>relay README</link> for the full setup, environment variables, and reverse-proxy guidance):",
    "s9Sub2CodeLabel": "On your relay host",
    "s9Sub2P3": "The <code>-v remote-pi-data:/data</code> mount is required — that's where the relay keeps <code>mesh.db</code> (the Owner-signed mesh blobs). Skip the volume and the table is wiped on every container restart, forcing every client to re-publish.",
    "s9Sub2P4": "The relay serves the WebSocket upgrade, <code>/health</code>, and <code>/mesh/'<owner_pk_hash>'</code> on the same port (default 3000) — point your reverse proxy at one upstream and you're done. Use <code>/health</code> for liveness probes (Coolify, Kubernetes, Fly health checks).",
    "s9Sub2P5": "Bind the container to your VPN interface, terminate TLS in a reverse proxy, and point both your Pi and your phone at the resulting <code>https://…</code> URL.",
    "s9Sub3Title": "Pointing Pi at your own relay",
    "s9Sub3P1": "Once your relay is reachable, tell the extension:",
    "s9Sub3CodeLabel1": "In Pi",
    "s9Sub3P2": "The URL must be <code>http://</code> or <code>https://</code> — <code>wss://</code> / <code>ws://</code> are rejected at validation. The extension converts to the WebSocket form internally when it opens the connection, so you can paste whatever URL your reverse proxy or PaaS dashboard exposes.",
    "s9Sub3P3": "This writes <code>~/.pi/remote/config.json</code> with <code>'{ \"relay\": \"...\" }'</code>. Resolution order (highest precedence first):",
    "s9Sub3Ol1": "<code>REMOTE_PI_RELAY</code> environment variable (CI / one-off overrides)",
    "s9Sub3Ol2": "<code>~/.pi/remote/config.json</code>",
    "s9Sub3Ol3": "The built-in default (<code>https://relay-rp1.jacobmoura.work</code>)",
    "s9Sub3P4": "Verify the active URL and its source with:",
    "s9Sub3CodeLabel2": "In Pi",
    "s9Sub3P5": "To switch URLs while connected: <code>/remote-pi stop</code> then <code>/remote-pi</code> again. The mobile app has its own relay-URL setting in its preferences pane — keep both pointing at the same relay.",
    "s10Title": "Protocol & Security",
    "s10P1": "The canonical spec for everything wire-level — envelope format, identity model (Owner key + per-device subkeys), ACK protocol, cross-PC routing, mesh membership, trust model, and failure modes — lives in <link>PROTOCOL.md</link> on GitHub. It is the source of truth that the Pi extension, the mobile apps, and the relay all implement against. Read it when you need exact behavior or when writing a new harness adapter.",
    "s10P2": "<b>Security posture, in short.</b> Connections to the relay are <b>encrypted in transit (TLS)</b>, and devices authenticate each other with <b>Ed25519 pairing</b>, so paired peers verify identity cryptographically. Message payloads are <b>not</b> encrypted at the application layer — the relay operator could in principle read plaintext in memory while forwarding. If you need confidentiality from the relay operator, <link>self-host the relay</link> behind a VPN. The Privacy Policy, section 9, restates this in plain language, and PROTOCOL.md is the deep dive that matches the code.",
    "s11Title": "Command reference",
    "s11P1": "Every command works as a Pi slash command (interactive) and as a shell-level <code>remote-pi '<subcommand>'</code> when the package is installed globally (<code>npm install -g remote-pi</code>).",
    "tableCommand": "Command",
    "tableDescription": "Description",
    "s11Sub1Title": "Local session — one Pi, one terminal",
    "s11Sub1Row1": "Connect (join local mesh + start relay), or run setup on first use",
    "s11Sub1Row2": "Run the setup wizard and update local config",
    "s11Sub1Row3": "Show local mesh + relay status",
    "s11Sub1Row4": "List local and cross-PC mesh peers, grouped by PC label",
    "s11Sub1Row5": "Stop everything for <em>this</em> terminal (mesh + relay)",
    "s11Sub1Row6": "Show QR + copy-paste pairing URI for a new mobile device (QR valid 60s by default; --ttl clamps to 10–600s)",
    "s11Sub1Row7": "List paired mobile devices (online/offline per device)",
    "s11Sub1Row8": "Revoke a paired device by its shortid (brings the relay up first, like pair, to notify the device)",
    "s11Sub1Row9": "Persist a new relay URL (http:// or https://)",
    "s11Sub2Title": "Daemon fleet — one supervisor, N background Pis",
    "s11Sub2P1": "See <link1>Daemon mode</link1> for the overview and the <link2>Daemon mode tutorial</link2> for the full how-to.",
    "s11Sub2Row1": "Register a folder as a daemon (starts it when the supervisor is running)",
    "s11Sub2Row2": "Unregister a daemon (local config preserved)",
    "s11Sub2Row3": "List registered daemons + state",
    "s11Sub2Row4": "Start one daemon by id, or every registered daemon with no id",
    "s11Sub2Row5": "Stop one daemon by id, or every running daemon with no id (<code>/remote-pi stop</code> stops only the local terminal)",
    "s11Sub2Row6": "Restart one daemon by id, or every daemon with no id",
    "s11Sub2Row7": "Detailed runtime status (pid, uptime, restart count)",
    "s11Sub2Row8": "Send a prompt to a specific daemon",
    "s11Sub2Row9": "Install <code>pi-supervisord</code> as a system service <b>and</b> symlink the <code>remote-pi</code> CLI into <code>~/.local/bin/</code>",
    "s11Sub2Row10": "Remove the system service <b>and</b> the <code>~/.local/bin</code> symlinks (daemon registry preserved)",
    "s11Sub3Title": "Daemon cron — scheduled prompts",
    "s11Sub3P1": "Schedule recurring prompts to a daemon. The scheduler runs <b>inside the supervisor</b>, so it only fires when the supervisor is installed as a service (<link1>Daemon mode</link1> / <code>/remote-pi install</code>) — otherwise <code>cron add</code> warns instead of scheduling. See the <link2>Daemon mode tutorial</link2> for the walkthrough.",
    "s11Sub3Row1": "Schedule a recurring prompt to a daemon (5-field cron expression; runs must be ≥60s apart)",
    "s11Sub3Row2": "List jobs: schedule, enabled, last run / status, next run",
    "s11Sub3Row3": "Fire a job now, ignoring its schedule",
    "s11Sub3Row4": "Resume a paused job",
    "s11Sub3Row5": "Pause a job without deleting it",
    "s11Sub3Row6": "Delete a job",
    "s11Sub3Row7": "Tail the fire / skip audit log (default N = 20), optionally for one job",
    "s11Sub3Li1": "<b>Minimum interval 60s.</b> A more frequent expression is rejected (each fire spends tokens). Croner syntax — five fields (<code>min hour day-of-month month day-of-week</code>), plus an optional seconds field, still bounded by the 60s floor.",
    "s11Sub3Li2": "<b>Timezone per job.</b> <code>--tz Area/City</code> resolves DST; without it a job runs in the machine's local time.",
    "s11Sub3Li3": "<b>Overlap & downtime.</b> A fire is skipped while the daemon is mid-turn (<code>--no-skip-busy</code> overrides), skipped if the daemon is down (<code>--wake</code> starts it first), and at most one missed run is replayed on startup (<code>--catchup</code>, off by default).",
    "s11Sub3Li4": "<b>Audit.</b> Every fire <em>and</em> every skip appends one line to <code>~/.pi/remote/cron.jsonl</code> with a <code>result</code> of <code>delivered</code>, <code>deliver_failed</code>, <code>woke_and_delivered</code>, <code>skipped_busy</code>, <code>skipped_down</code>, or <code>skipped_disabled</code>. The agent's reply itself is fire-and-forget into the mesh — cron records the trigger, not the response.",
    "s11FooterP1": "The footer in the Pi TUI reflects state live:",
    "s11FooterLi1": "<code>📡 local (N)</code> — local mesh session and peer count",
    "s11FooterLi2": "<code>🟢 relay</code> — relay connected, at least one device paired on this machine",
    "s11FooterLi3": "<code>🟡 relay waiting for pairing</code> — relay connected, no device paired yet",
    "s11FooterLi4": "<code>📱 '<shortid>'</code> — a mobile device is actively connected right now",
    "s11FooterP2": "The window title is two parts — <code>'<agent-name>' · On</code> when the relay is up or <code>'<agent-name>' · Off</code> otherwise — so you can tell your terminal tabs apart at a glance.",
    "s12Title": "Configuration files",
    "tablePath": "Path",
    "tableScope": "Scope",
    "tableWhatsInIt": "What's in it",
    "s12ScopePerDirectory": "Per-directory",
    "s12ScopePerUser": "Per-user",
    "s12ScopePerMachine": "Per-machine",
    "s12Row3Desc": "Paired mobile devices",
    "s12Row4Desc": "Daemon registry (list of <code>'{ cwd, name }'</code> entries)",
    "s12Row5Desc": "Pi-secret fallback when the OS keyring is unavailable (headless Linux). Stored with <code>chmod 0600</code>. See <link>PROTOCOL.md</link> for the keyring details.",
    "s12Row6Desc": "Broker socket + <code>audit.jsonl</code>",
    "s12Row7Desc": "Agent skill the LLM reads",
    "s12P1": "Override the relay for a single run without persisting:",
    "s12CodeLabel1": "Shell",
    "s12P2": "Only <code>http://</code> / <code>https://</code> are accepted — <code>wss://</code> / <code>ws://</code> are rejected at validation, the extension converts to the WebSocket form internally when it opens the connection.",
    "s12P3": "Daemons don't read the per-directory file at all — the supervisor injects their whole config inline via the <code>REMOTE_PI_DIRECT_CONFIG</code> environment variable at spawn (a fixed <code>assistent</code> workspace with the relay on), so a daemon folder needs no <code>.pi/remote-pi/</code> of its own. You can set the same variable yourself to override <code>'<cwd>'/.pi/remote-pi/config.json</code> for a single run — an escape hatch for CI, ops, and the Cockpit desktop client:",
    "s12CodeLabel2": "Shell",
    "s13Title": "Troubleshooting",
    "s13Sub1Title": "Footer says 🟡 relay waiting for pairing even though I paired a device",
    "s13Sub1P1": "The icon reflects whether <em>any</em> device has been paired on this machine, not whether one is connected right now. If you really have a paired device in <code>/remote-pi devices</code>, restart Pi — the cache may be stale (fixed in current release; report a bug if it recurs).",
    "s13Sub2Title": "Mobile app times out connecting",
    "s13Sub2P1": "Verify the same relay URL is configured on both sides. If you self-host behind a VPN, your phone must also be on the VPN (Tailscale on iOS/Android works fine).",
    "s13Sub3Title": "Reply never arrives",
    "s13Sub3P1": "<code>agent_send</code> returned <code>'{ status: \"received\" }'</code> but no reply ever lands in your inbox. Possible causes:",
    "s13Sub3Li1": "<b>Receiver crashed or never processed.</b> Run <code>/remote-pi peers</code> to see whether the peer is still online.",
    "s13Sub3Li2": "<b>The receiver chose not to reply.</b> <code>agent_send</code> is not RPC — there's no obligation to respond. If the conversation needs a reply, the prompt to the receiver must say so explicitly.",
    "s13Sub3Li3": "<b>Cross-PC: peer went offline.</b> Look in your inbox for a <code>transport_error</code> envelope with <code>re='<your-send-id>'</code> — the relay returns one when a forwarded message can't be delivered. A <code>Delivered</code> ACK only means the remote broker accepted the envelope, not that the peer is alive — validate by roundtrip.",
    "s13Sub3P2": "<b>Note:</b> <code>agent_request</code> is deprecated (still available as a wrapper for backward compat, emits a warning). New agents call <code>agent_send</code> and observe the inbox in a future turn.",
    "s13Sub4Title": "Two Pi processes can't share a directory",
    "s13Sub4P1": "A cwd lock allows <b>one Pi process per directory</b>. If you try to run <code>/remote-pi</code> in a second terminal that's already in the same folder, the second start is rejected (and the relay, separately, refuses a duplicate room with <code>RoomAlreadyOpenError</code>).",
    "s13Sub4P2": "<b>To run two agents side by side:</b> put them in two different directories — each gets its own workspace and both meet in the same <code>local</code> session. See the <link>Local mesh tutorial</link>.",
    "s13Sub4P3": "If you actually wanted a second terminal at the same workspace (e.g. just to read state), stop the running Pi first or open a shell that does <em>not</em> launch Pi.",
    "s14Title": "Links",
    "s14Li1Label": "Homepage:",
    "s14Li2Label": "Tutorials:",
    "s14Li2Text": "hands-on guides",
    "s14Li3Label": "Cockpit reference:",
    "s14Li3Text": "CLI, layouts, tasks, themes",
    "s14Li4Label": "Source:",
    "s14Li5Label": "Protocol spec:",
    "s14Li6Label": "Pi coding agent:",
    "s14Li7Label": "Relay (self-hosting guide):",
    "s14Li8Label": "Issues / bugs:",
    "s14License": "License: MIT."
  },
  "CockpitDocsPage": {
    "title": "Cockpit reference",
    "metaDescription": "Reference for Remote Pi Cockpit: the internal CLI, .ckp pane layouts, .cockpit/tasks.json Task Run, the theme format, agent turn status hooks, and databases.",
    "eyebrow": "Documentation · Cockpit",
    "h1": "Cockpit reference",
    "lastUpdated": "Last updated: 2026-08-11",
    "license": "License: MIT",
    "lede": "Everything the desktop app reads from your repository or writes to your machine: the internal <code>cockpit</code> CLI that agents use to drive tabs, the <code>.ckp</code> pane layouts, the <code>.cockpit/tasks.json</code> Task Run file, the theme format, and the harness hooks behind the turn status. For the product tour, see the <cockpitLink>Cockpit page</cockpitLink>; for the mesh, daemons, and the relay, see the <remotePiDocsLink>Remote Pi docs</remotePiDocsLink>.",
    "tocInstall": "Install",
    "tocCli": "The",
    "tocCliTargets": "Targets & ids",
    "tocCliCommands": "Command reference",
    "tocCliRead": "Reading output",
    "tocLayouts": "pane layouts",
    "tocLayoutsFields": "Fields",
    "tocLayoutsMerge": "Merge semantics",
    "tocTasks": "Task Run",
    "tocTasksFields": "Fields",
    "tocDatabases": "Databases",
    "tocThemes": "Themes",
    "tocThemesFile": "Theme file",
    "tocThemesTokens": "Tokens",
    "tocTurnStatus": "Agent turn status",
    "tocTurnStatusEvents": "Event mapping",
    "tocTurnStatusResume": "Resuming a session",
    "tocSounds": "Sounds & notifications",
    "tocLanguage": "Language",
    "tocLinks": "Links",
    "s1Title": "Install",
    "s1P1": "Cockpit ships for macOS, Windows, and Linux. macOS builds are signed and notarized, and every platform that supports it gets in-app updates. Grab a build from the <link>download page</link> — <code>.dmg</code>, <code>.exe</code>, <code>.deb</code> and <code>.rpm</code> (x64 and arm64), each with a published SHA-256.",
    "s1P2": "Cockpit is a terminal first: it needs no account, no cloud, and no Remote Pi setup to be useful. The mesh, pairing, and daemon features light up only once you install the <code>remote-pi</code> extension described in the <link>main docs</link>.",
    "s2Title": "The cockpit CLI",
    "s2P1": "Cockpit materializes a small binary at <code>~/.cockpit/bin/cockpit</code> and puts that folder on the <code>PATH</code> of the terminals it spawns — and only those. So the CLI exists for anything running inside Cockpit (you, a script, an agent) and does not leak into the rest of your shell environment. It talks to the app over a local socket: a Unix socket on macOS and Linux, a loopback TCP port plus a token on Windows.",
    "s2P2": "This is what makes Cockpit an <em>agentic</em> multiplexer: an agent in one tab can open another tab, type into it, read what it printed, run a project task, or query a database — the same verbs a human uses, with no screen scraping.",
    "tableFlag": "Flag",
    "tableWhatItDoes": "What it does",
    "tableCommand": "Command",
    "tableDefault": "Default",
    "tableField": "Field",
    "tableRequired": "Required",
    "tableDescription": "Description",
    "tableWhatItIs": "What it is",
    "tableGroup": "Group",
    "tableTokens": "Tokens",
    "tableHarness": "Harness",
    "tableFile": "File",
    "tableFormat": "Format",
    "tableEvent": "Event",
    "tableStatus": "Status",
    "yes": "yes",
    "no": "no",
    "off": "off",
    "all": "all",
    "root": "root",
    "metadata": "Metadata.",
    "ignored": "ignored",
    "s2Sub1Title": "Targets & ids",
    "s2Sub1P1": "The unit the CLI addresses is a <b>tab</b> (one terminal or agent session). A <b>pane</b> is the split leaf that groups tabs and is not addressable — <code>list-panes</code> and <code>read-pane</code> survive as legacy aliases.",
    "s2Sub1Row1": "Target another tab. Defaults to <code>$COCKPIT_TAB_ID</code> (the current tab; legacy fallback <code>$COCKPIT_PANE_ID</code>).",
    "s2Sub1Row2": "Target whatever tab you are looking at, resolved by the app. Works from outside a Cockpit terminal too (dictation tools, scripts): with no env inherited the CLI finds the app through <code>~/.cockpit/status.sock</code>. Wins over <code>--tab-id</code>.",
    "s2Sub1Row3": "(<code>send</code> only) press Enter right after the text, as a separate keystroke — a <code>send</code> plus a <code>send-key Enter</code> in one call.",
    "s2Sub1CalloutTitle": "Tab ids reset on boot",
    "s2Sub1CalloutBody": "Ids (<code>t0</code>, <code>t1</code>…) are assigned per app boot, so never hardcode one. Discover them with <code>cockpit list-tabs</code>, or give a tab a <b>stable label</b> (double-click the tab, or <code>new-tab --title</code>) and address it by name. Labels persist across boots; workspace ids are opaque UUIDs, so use <code>workspacePath</code> / <code>path</code> when you need the folder on disk.",
    "s2Sub2Title": "Command reference",
    "s2Sub2Row1": "Type text into a tab.",
    "s2Sub2Row2": "Press named keys: Enter Tab Escape Space BSpace Up Down Left Right Home End PageUp PageDown Delete and <code>C-'<letter>'</code> (e.g. <code>C-c</code>).",
    "s2Sub2Row3": "Open a file in the app's viewer. Bare <code>cockpit '<file>'</code> is a shortcut for it, relative to the tab's cwd.",
    "s2Sub2Row4": "Open a terminal tab and print its id. <code>h</code>/<code>right</code> splits side by side, <code>v</code>/<code>down</code> stacks; omit to open as a tab in the same pane. Anchored at the emitting tab's pane.",
    "s2Sub2Row5": "Read a tab's rendered output. No target = the current tab. Alias: <code>read-pane</code>.",
    "s2Sub2Row6": "Read a task's output, even with no tab open for it.",
    "s2Sub2Row7": "List active tabs (alias: <code>list-panes</code>). The JSON carries <code>label</code>, <code>workspacePath</code>, <code>working</code> and, for task output tabs, <code>taskId</code>.",
    "s2Sub2Row8": "List workspaces (projects) and their paths.",
    "s2Sub2Row9": "List this workspace's tasks. <code>[output]</code> marks tasks whose output <code>read-task</code> can read (ran this boot); ● marks tasks running right now.",
    "s2Sub2Row10": "SQL databases registered in the workspace — see <link>Databases</link>.",
    "s2Sub2Row11": "Run a Redis command, or open the key table for a human.",
    "s2Sub2Row12": "Run a MongoDB command, or open the collection browser.",
    "s2Sub2Row13": "Apply a pane layout — see <link><code>.ckp</code> layouts</link>.",
    "s2Sub2Row14": "Install the Claude Code skill that teaches this CLI.",
    "s2Sub2CodeLabel": "Cockpit terminal",
    "s2Sub3Title": "Reading output",
    "s2Sub3P1": "<code>read-tab</code> and <code>read-task</code> share a windowing model. Output is always chronological (top to bottom); the flags only pick which window you get.",
    "s2Sub3Row1": "How many lines to return (server cap: 2000).",
    "s2Sub3Row2": "Skip N lines from the anchor — this is your pagination.",
    "s2Sub3Row3": "Anchor at the start of the buffer instead of the tail.",
    "s2Sub3P2": "Task ids are stable per workspace: <code>npm:'<script>'</code> from <code>package.json</code>, <code>flutter:run</code> / <code>flutter:test</code>, and <code>json:'<label>'</code> from <code>.cockpit/tasks.json</code>.",
    "s3P1": "A <code>.ckp</code> file is a versionable YAML that describes the terminals to open in a workspace — the equivalent of a tmuxinator layout. One file is one layout, and the file name is the layout name (<code>dev.ckp</code> → layout “dev”). Commit it, and a teammate gets your working geometry on clone.",
    "s3P2": "There are three ways to apply one:",
    "s3Li1": "<b>GUI</b> — right-click the <code>.ckp</code> file in the tree → <b>Open layout</b>.",
    "s3Li2": "<b>CLI</b> — <code>cockpit orchestrate dev.ckp</code> from inside a tab.",
    "s3Li3": "<b>Worktree autorun</b> — <code>autorun: worktree</code> in the file: the layout is applied by itself whenever you create a worktree of the workspace. The worktree is born empty, so the geometry comes out exact.",
    "s3Sub1Title": "Fields",
    "s3Sub1P1": "<b>Root:</b> <code>panes</code> (required) is the list of panes in creation order. <code>autorun</code> (optional) accepts only <code>worktree</code>; with two or more autorun files at the root, none of them runs — ambiguity is never guessed.",
    "s3Sub1Row1": "Unique (case-insensitive). Becomes the tab's manual label and the merge key.",
    "s3Sub1Row2": "Relative to the file's folder, forward slashes only. Absolute paths and <code>\\</code> are rejected for portability.",
    "s3Sub1Row3": "Where it is born, relative to the <em>previously created</em> pane: <code>tab</code>, <code>right</code>, <code>down</code>.",
    "s3Sub1Row4": "Typed into the terminal and run by the tab's shell (resolved through the machine's PATH).",
    "s3Sub1Row5": "<code>macos</code> / <code>windows</code> / <code>linux</code>, string or list — same semantics as in <code>tasks.json</code>.",
    "s3Sub2Title": "Merge semantics",
    "s3Sub2Li1": "A pane whose <code>name</code> already exists as a tab label or title in the workspace is <b>skipped</b> — applying a layout twice is a no-op. Nothing is ever closed.",
    "s3Sub2Li2": "<code>split</code> anchors on the pane created previously <em>in this run</em>; if that one was skipped by the merge, the next opens as a plain tab. Perfect geometry is guaranteed only in an empty workspace — which is exactly the worktree autorun case.",
    "s3Sub2Li3": "A missing <code>cwd</code> or invalid YAML gives a readable error (dialog in the GUI, stderr in the CLI); nothing is applied halfway from the failing pane on.",
    "s3Sub2Li4": "<code>command</code> is typed ~700 ms after the tab opens, so the shell has time to finish booting, with Enter at the end.",
    "s3Sub2P1": "Cockpit treats <code>.ckp</code> as YAML for highlighting and shows the Cockpit logo as the file icon in the tree.",
    "s4Title": "Task Run",
    "s4P1": "Task Run executes your project's build and dev commands (<code>npm run dev</code>, <code>flutter run</code>, <code>go run</code>, <code>make</code>…) with streamed output, a visual lifecycle (play / stop / restart), interactive keys, and reload-on-save. Two sources coexist:",
    "s4Li1": "<b>Auto-detected</b> — on opening a project, Cockpit reads the manifests (<code>package.json</code> scripts, <code>pubspec.yaml</code>) and shows tasks with no config at all.",
    "s4Li2": "<b>Declared</b> — <code>.cockpit/tasks.json</code>, for customizing, adding tasks, or describing a monorepo. JSON tasks take precedence over a detected task with the same id.",
    "s4CalloutTitle": "The runner is generic",
    "s4CalloutBody": "It knows only <code>command</code>, <code>args</code>, and <code>env</code>. There are no stack-specific keys (flavor, dart-define, <code>NODE_ENV</code>) — all of that is expressed as <code>args</code> and <code>env</code>.",
    "s4Sub1Title": "Where the file lives",
    "s4Sub1P1": "At the <b>root of the workspace you open</b>. Discovery is literal — Cockpit does not walk up the tree. For a single package, open the package folder. For a <b>monorepo</b>, open the root and let one <code>.cockpit/tasks.json</code> drive the subpackages through a per-task <code>cwd</code>.",
    "s4Sub1P2": "The file is <b>JSONC</b>: comments (<code>//</code> and <code>/* */</code>) and trailing commas are allowed, just like VSCode's <code>tasks.json</code>. Point <code>$schema</code> at <link><code>docs/tasks.schema.json</code></link> for editor autocomplete; Cockpit ignores the field when running.",
    "s4Sub2Title": "Fields",
    "s4Sub2P1": "Root: <code>tasks</code> (required) and <code>cwd</code> (optional) — a default <code>cwd</code> for every task, which each task may override.",
    "s4Sub2Row1": "Short display name. The task id is derived from it: <code>json:'<label>'</code>.",
    "s4Sub2Row2": "Base executable (npm, flutter, dart…).",
    "s4Sub2Row3": "Base args, placed before the profile's args.",
    "s4Sub2Row4": "Run folder, relative to the <code>tasks.json</code> folder (absolute also accepted). Falls back to the top-level <code>cwd</code>, then the root.",
    "s4Sub2Row5": "OSes where the task is visible: <code>macos</code>, <code>windows</code>, <code>linux</code>.",
    "s4Sub2Row6": "<code>watch</code> (long-lived process, e.g. a dev server) or <code>oneShot</code> (runs and exits).",
    "s4Sub2Row7": "Buttons that write to the process' stdin: <code>key</code>, <code>label</code>, optional <code>icon</code> (<code>refresh</code>, <code>restart</code>, <code>stop</code>, <code>bolt</code>) and <code>primary</code>.",
    "s4Sub2Row8": "Reload on save: <code>paths</code>, <code>ignore</code>, <code>onChange</code> (required — an <code>interactiveKeys</code> label or <code>\"__restart__\"</code>) and <code>debounceMs</code> (300). Leave it out for tools that already watch (Vite, Next).",
    "s4Sub2Row9": "<code>begin</code> / <code>end</code> regexes that swing the badge between <em>building</em> and <em>running</em>.",
    "s4Sub2Row10": "Named run variants (launch configs): <code>name</code>, extra <code>args</code> appended after the task's, and <code>env</code> merged into the process environment. A chip cycles them before you hit play.",
    "s4Sub2CalloutTitle": "Two known limits",
    "s4Sub2CalloutBody": "For an argument value containing spaces, use <b>separate items</b> in <code>args</code> (<code>[\"--dart-define\", \"MSG=hello world\"]</code>). And the output tab does not survive an app restart — the task dies with it.",
    "s5Title": "Databases",
    "s5P1": "Connections live per workspace in <code>.cockpit/databases.json</code>, plus any SQLite files Cockpit auto-detects in the project. SQLite, Postgres, MySQL, SQL Server, Redis, and MongoDB open as tabs: a table view for SQL and Redis, a collection browser for MongoDB. A <code>.dbq</code> file is a saved query you can commit and re-run.",
    "s5P2": "The same connections are reachable from the CLI, and the output is <b>one JSON line</b> — built to be parsed by an agent, not read by a human.",
    "s5CalloutTitle": "Per-connection guardrails",
    "s5CalloutBody": "Each connection carries an <code>access</code> level (<code>read</code> — the default, including for connections created before the field existed — or <code>readwrite</code>) and an <code>agents</code> flag. A connection with <code>agents: false</code> is invisible to the CLI. The gates are enforced on the CLI surface: what you do by hand in the GUI is never blocked, but what an agent can reach is yours to decide. Run <code>cockpit db --help</code> for the full surface.",
    "s6Title": "Themes",
    "s6P1": "Cockpit ships nine built-in themes — <code>cockpit</code>, <code>cockpit.2</code>, <code>violet</code>, <code>violet.2</code>, <code>midnight</code>, <code>rose</code>, <code>sun</code>, <code>flexoki</code> and <code>pantera</code> — each with a light and a dark variant. Beyond those, a theme is <b>a single JSON file</b> that paints all three layers at once: the app UI, the code viewer's syntax highlighting, and the terminal palette.",
    "s6Sub1Title": "The theme file",
    "s6Sub1Li1": "Import with <b>Settings → Appearance → Theme → Import…</b>",
    "s6Sub1Li2": "Themes live in <code>'<data folder>'/themes/</code> (the same root as the “Storage” setting), one file per theme, named after its <code>id</code>. Copying a <code>.json</code> in there installs it too.",
    "s6Sub1Li3": "Export produces a <b>complete</b> file (every token, no <code>extends</code>) — a good starting point for hand editing.",
    "s6Sub1Row1": "Stable, namespaced identity (<code>publisher.name</code>). It is what gets stored in preferences, so renaming <code>name</code> never loses the user's choice. Cannot collide with a built-in id.",
    "s6Sub1Row2": "What shows up in the picker.",
    "s6Sub1Row3": "Id of a built-in theme to inherit from. Absent = inherits from <code>cockpit</code>.",
    "s6Sub1Row4": "At least one of <code>dark</code> / <code>light</code>. A dark-only theme is applied in light mode too — better than mixing half a light theme with half a dark one.",
    "s6Sub1P1": "<b>Inheritance is the point.</b> Every token you do not declare comes from the base, so a useful theme can be five lines long:",
    "s6Sub1P2": "Colors are CSS-style hex — <code>#RGB</code>, <code>#RRGGBB</code> or <code>#RRGGBBAA</code>, <b>alpha last</b>. It is not Dart's <code>0xAARRGGBB</code>. On a bad import the parser points at the <b>field path</b> that broke (<code>variants.dark.ui.accent</code>), and validation runs before the copy, so an invalid file never reaches the themes folder.",
    "s6Sub2Title": "Tokens",
    "s6Sub2Row1": "Surfaces <code>bg</code> <code>panel</code> <code>panel2</code> <code>panel3</code> · strokes <code>border</code> <code>border2</code> · text <code>text</code> <code>text2</code> <code>text3</code> <code>text4</code> · brand <code>accent</code> <code>accentSoft</code> <code>accentText</code> · state <code>online</code> <code>ok</code> <code>error</code> <code>warn</code> · editing <code>edited</code> <code>editedBg</code> · git <code>gitStaged</code> <code>gitUntracked</code> <code>gitDeleted</code> <code>gitConflict</code> · overlay <code>scrim</code> <code>shadow</code>",
    "s6Sub2Row2": "<code>background</code> <code>base</code> <code>comment</code> <code>keyword</code> <code>string</code> <code>number</code> <code>class</code> <code>builtin</code> <code>function</code> <code>variable</code> <code>meta</code> <code>deletion</code>",
    "s6Sub2Row3": "<code>cursor</code> <code>selection</code> <code>foreground</code> <code>background</code>, the 8 normal ANSI colors, their 8 <code>bright*</code> counterparts, and <code>searchHitBackground</code> <code>searchHitBackgroundCurrent</code> <code>searchHitForeground</code>",
    "s6Sub2P1": "Text drawn <em>on top of</em> <code>accent</code> and <code>error</code> is not a token: it is derived from the color's luminance, so a light accent automatically gets dark text.",
    "s6Sub2P2": "<code>syntax.background</code> has a special default. The code viewer, the editor, and the terminal are all content inside a tab, so they share the field: when a theme declares neither <code>syntax.background</code> nor <code>terminal.background</code>, the three follow <code>ui.panel</code> of <em>this</em> theme, not of the base. Declare the field if your code palette needs a surface of its own.",
    "s6Sub2P3": "The <code>$schema</code> URL lives in the repository, versioned next to the code that implements it, so the app and the schema can never drift apart. Cockpit ignores the field when reading a theme — it only serves your editor. See <link><code>theme.example.json</code></link> for a commented file with every group filled in.",
    "s7Title": "Agent turn status",
    "s7P1": "When an agent runs in a tab, Cockpit shows whether it is <b>working</b>, <b>waiting</b> for you, or <b>idle</b> — as a spinner, a badge, a chime, and an OS notification when the window is unfocused. Supported harnesses today: <b>Claude Code</b> and <b>Codex CLI</b> (0.147+).",
    "s7P2": "How it works:",
    "s7Ol1": "At boot, Cockpit materializes the internal CLI at <code>~/.cockpit/bin/cockpit</code> and registers <code>cockpit hook</code> on the harness' lifecycle events.",
    "s7Ol2": "On each event the harness runs the hook, passing a JSON payload on stdin.",
    "s7Ol3": "The hook translates it into a status and sends it to the app over the local socket. (A socket rather than an escape sequence on the PTY: harnesses run hooks with no controlling terminal, and writing to <code>/dev/tty</code> fails with <code>ENXIO</code>.)",
    "s7Ol4": "Routing is by the <code>COCKPIT_PANE_ID</code> env var, which the app injects into the tab's PTY. An agent session started <em>outside</em> Cockpit does not have it, so the hook is a no-op there. Nothing to configure, nothing to disable.",
    "s7Row1": "<code>hooks.'<Event>'[]</code>, each item '{matcher, hooks:[{type, command}]}'",
    "s7Row2": "Same shape, <b>plus</b> a trust block in <code>~/.codex/config.toml</code> (Codex silently ignores an untrusted hook, so Cockpit computes and writes the trust hash for you, between <code># >>> cockpit hooks</code> delimiters).",
    "s7P3": "Both installers do an <b>idempotent append</b> of a marked entry (<code>_cockpit: v1</code>): re-running removes our old entry and re-adds it, never rewriting the list — your own hooks, and those of plugins or iTerm2, survive untouched.",
    "s7Sub1Title": "Event mapping",
    "s7Sub1Row1": "<code>working</code> (turn starts)",
    "s7Sub1Row2": "<code>working</code> — except for Claude's blocking tools (below)",
    "s7Sub1Row4": "<code>waiting</code> / <code>idle</code> (heuristic on the text)",
    "s7Sub1P1": "Two asymmetries matter. On <b>Claude</b>, tools that block waiting for the user (<code>AskUserQuestion</code>, <code>ExitPlanMode</code>) emit no <code>Notification</code>; the last hook before the block is <code>PreToolUse</code>, so that one maps to <code>waiting</code> for those two — otherwise the tab would spin forever with no chime. On <b>Codex</b>, approval has its own event, so there is no text heuristic and no detour.",
    "s7Sub2Title": "Resuming a session",
    "s7Sub2P1": "Cockpit persists the <code>session_id</code> that arrived through the hook, and on restoring the tab it types the command that reattaches the conversation. A session id alone does not say which harness it belongs to, and the commands differ — <code>claude --resume '<id>'</code> versus <code>codex resume '<id>'</code> — so the installer registers the hook as <code>cockpit hook --harness '<name>'</code> and the layout stores the harness next to the id. Entries written by older versions pass no flag and are assumed to be Claude, which is what they all were.",
    "s7Sub2CalloutTitle": "Codex trust is index-keyed",
    "s7Sub2CalloutBody": "The Codex trust key includes the hook's group index. If you add a hook of your own <em>before</em> ours on the same event — or edit <code>hooks.json</code> by hand — the hash stops matching and the hook silently stops running. The installer repairs it on the next boot, because it recomputes the indices from the final file.",
    "s8Title": "Sounds & notifications",
    "s8P1": "Turn status drives audio too. Under <b>Settings → Notifications</b> you can bind a sound per event — <b>turn done</b>, <b>action needed</b>, <b>error</b> — pick a custom audio file for each, and set the volume. With the window focused you get the chime; unfocused, an OS notification. Since an agent started outside Cockpit never reports status, nothing fires for sessions the app is not hosting.",
    "s9Title": "Language",
    "s9P1": "Cockpit's interface is fully localized in <b>English</b>, <b>Portuguese (Brazil)</b> and <b>Spanish</b>, down to the native application menu. Switch it in <b>Settings → General → Language</b>; the choice is independent of the OS locale.",
    "s10Title": "Links",
    "s10Li1Text": "Cockpit product page",
    "s10Li1Suffix": "the tour.",
    "s10Li2Text": "Tutorial: layouts and tasks",
    "s10Li2Suffix": "build a <code>.ckp</code> and a <code>tasks.json</code> from scratch.",
    "s10Li3Text": "Tutorial: an agent team in Cockpit",
    "s10Li4Text": "Remote Pi docs",
    "s10Li4Suffix": "mesh, relay, daemons, pairing.",
    "s10Li5Text": "Source docs & JSON schemas",
    "s10Li5Suffix": "in the repository.",
    "s10Li6Suffix": "issues and source."
  }
}
